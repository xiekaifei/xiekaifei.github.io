<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>2019春运回家路</title>
    <url>/2019/gohome.html</url>
    <content><![CDATA[<p>&emsp;&emsp;转眼间又要过年了，又到了号称“人类史上最大规模迁徙”的春运，而我自从毕业参加工作以后，也是每年都参与其中。今年是我工作地距离家最远的一次，重庆到张家口宣化，地图显示开车到家需要20小时左右，1733公里，因为我还没有车（当然即使有也不会这么远开车回家），所以早早的也加入了抢票大军。今年的假期还算比较长，年假、假期加上老板今年心情美丽（这里要感谢老板），额外给了三天带薪年假，算下来一共有20天的时间，可以好好的回家耍一耍了。</p>
<p>&emsp;&emsp;先说一下行程，重庆到宣化和张家口都没有直达的火车，所以必须从离家比较近的北京中转，路程就变为了“重庆—北京—宣化”。原本计划的是坐火车回家，也提前在智行上抢到了两程的火车票。某天在小胖妹儿的提醒下看了机票，正好看到海航有重庆至北京的特价机票，赶紧退了重庆到北京的火车票，买了机票回家，算下来相比18个小时的火车票，坐飞机节省了15个半小时，而且当天就能到家，还是相当不错的。</p>
<a id="more"></a>
<p>&emsp;&emsp;回家这天是早上8点20的飞机，上午10点40到北京。由于住的地方离机场有些远，所以当天早上5点就起来了，洗漱完毕后，打车赶10号线最早的一班地铁，别问为什么，因为穷，打车到10号线的起始站鲤鱼池地铁站比直接打车到机场省了50块钱左右，用的时间还差不多，所以本着节俭朴素的原则还是选择这种方式到机场。</p>
<p>&emsp;&emsp;6点钟出门，天还没亮，平时小区门口一条热闹的美食街此时也是冷冷清清的。</p>
<p><img src="https://cdn.xiekaifei.com/image/2019/blog/shenghuo/IMG_20190123_060332.jpg" alt="小区门前的美食街"></p>
<p>&emsp;&emsp;穿过这条街前的马路上，倒是已经有不少汽车了，还能看到早早就出摊的卖早点的人，他们也是蛮辛苦的。</p>
<p><img src="https://cdn.xiekaifei.com/image/2019/blog/shenghuo/IMG_20190123_060725.jpg" alt="马路"></p>
<p>&emsp;&emsp;打车20分钟左右到了鲤鱼池地铁站，此时是6点半，正好赶上首班地铁。从地铁口下去的时候，下了五节下图这么长的电梯，网上查了一下，鲤鱼池地铁站埋深76米，相当于住宅楼27层高，不禁感叹我国的基建水平真的是突飞猛进。</p>
<p><img src="https://cdn.xiekaifei.com/image/2019/blog/shenghuo/IMG_20190123_062950.jpg" alt="重庆轨道交通10号线鲤鱼池站"></p>
<p><img src="https://cdn.xiekaifei.com/image/2019/blog/shenghuo/IMG_20190123_062929.jpg" alt="重庆轨道交通10号线鲤鱼池站"></p>
<p>&emsp;&emsp;40分钟左右从鲤鱼池站到了江北机场T3航站楼。今年回家比较早，是春运刚开始的第3天，没有到高峰期，但还是能看出来机场的人比以往要多一些。</p>
<p><img src="https://cdn.xiekaifei.com/image/2019/blog/shenghuo/IMG_20190123_071603.jpg" alt="重庆江北国际机场T3航站楼"></p>
<p>&emsp;&emsp;带的东西不算太多，只有一个20寸的登机箱，装了一些换洗衣服，安检也比较顺利。</p>
<p><img src="https://cdn.xiekaifei.com/image/2019/blog/shenghuo/IMG_20190123_071956.jpg" alt="机场安检"></p>
<p>&emsp;&emsp;8点前成功登机，这次坐的是海航的78A，也就是波音787-9，属于中长型宽体客机，经济舱采用的是3+3+3的座椅布局，相较于比较热门的中短程窄体客机空客A320和波音737要大了一些。目前民航中采用的基本都是空中客车和波音公司生产的客机，不知道什么时候能坐上中国的C919。登机以后天还没完全亮，我坐在靠窗正对机翼的位置，还可以看看窗外的风景。</p>
<p><img src="https://cdn.xiekaifei.com/image/2019/blog/shenghuo/IMG_20190123_074537.jpg" alt="登机"></p>
<p>飞行途中蓝蓝的天：</p>
<p><img src="https://cdn.xiekaifei.com/image/2019/blog/shenghuo/IMG_20190123_085413.jpg" alt="飞行途中"></p>
<p>&emsp;&emsp;这架飞机上是有wifi的，但是不知道为什么不能用，所以全程基本上是睡了过去。快到北京的时候天已经完全亮了。</p>
<p>首都机场上空准备降落：</p>
<p><img src="https://cdn.xiekaifei.com/image/2019/blog/shenghuo/IMG_20190123_102725.jpg" alt="首都机场上空"></p>
<p>&emsp;&emsp;大约10点40，到了首都机场T1航站楼。下午要到北京站去坐火车，准备坐地铁机场线过去，T1航站楼没有直接连通机场线，要走大约10分钟到T2航站楼坐车。</p>
<p><img src="https://cdn.xiekaifei.com/image/2019/blog/shenghuo/IMG_20190123_113605.jpg" alt="T1至T2途中"></p>
<p>&emsp;&emsp;由于时间还早，转车的时候到东直门附近转了转，在东直门桥上随手拍了一张，北京的太阳是真的好，不像重庆的冬天，很少能见到太阳。</p>
<p><img src="https://cdn.xiekaifei.com/image/2019/blog/shenghuo/IMG_20190123_123746.jpg" alt="东直门桥"></p>
<p>&emsp;&emsp;下午三点到了熟悉的北京站，唯一感觉就是人多，站前广场上站满了准备回家的人，就连各个候车室里也是人满为患，座位基本没有空出来的，还好一会就可以检票上车了，不用在车站停留很久。</p>
<p><img src="https://cdn.xiekaifei.com/image/2019/blog/shenghuo/IMG_20190123_145904.jpg" alt="北京站"></p>
<p><img src="https://cdn.xiekaifei.com/image/2019/blog/shenghuo/IMG_20190123_150615.jpg" alt="北京站候车厅内"></p>
<p>&emsp;&emsp;我坐的是北京到沙岭子西的Y537次列车，张家口南站由于冬奥会正在扩建，所以现在到张家口南站的车都改到距离此站大约10公里左右的沙岭子西站。张家口的机场目前也在扩建，今年的3月就可以恢复运营了，如果到时候能开通重庆到张家口的航班就好了，这样以后回家就方便太多。希望家乡能借冬奥会之势，发展的越来越好！</p>
<p><img src="https://cdn.xiekaifei.com/image/2019/blog/shenghuo/IMG_20190123_150753.jpg" alt="Y573次列车"></p>
<p>踏上回家的列车：</p>
<p><img src="https://cdn.xiekaifei.com/image/2019/blog/shenghuo/IMG_20190123_152025.jpg" alt="上车"></p>
<p>&emsp;&emsp;2个半小时的车程很快就过完了，老妈来到火车站接我，此时已经天黑了。回到家吃上了热乎的饭菜，和老爸老妈小酌几杯，说说一年当中的酸甜苦辣。</p>
<p>&emsp;&emsp;整整一天的回家路，奔波1900余公里，顺利到家。至此，旅途结束。最后，祝愿那些和我一样在外工作的人，都能在春运期间买到车票，顺利的走完回家的路！</p>
]]></content>
      <categories>
        <category>生活</category>
      </categories>
      <tags>
        <tag>春运</tag>
      </tags>
  </entry>
  <entry>
    <title>Hexo博客系统常用命令</title>
    <url>/2019/hexo-command.html</url>
    <content><![CDATA[<p>&emsp;&emsp;我的博客使用的是Hexo框架，可以很方便的使用一些命令编辑博文，发布文章或者本地预览等操作。但是每次写文章的时候，都会忘记一部分命令（主要原因博客写的太少…）导致每次都需要google查询这些操作的命令。这次干脆总结一下常用的命令，方便下次使用。</p>
<a id="more"></a>
<table>
<thead>
<tr>
<th>命令</th>
<th>操作</th>
</tr>
</thead>
<tbody>
<tr>
<td>hexo new <code>title</code></td>
<td>新建一篇文章。（如果标题包含空格的话，需要使用引号括起来。）省略了layout参数</td>
</tr>
<tr>
<td>hexo generate（可简写为hexo g）</td>
<td>生成静态文件，可加参数-d，生成静态文件后部署网站</td>
</tr>
<tr>
<td>hexo clean</td>
<td>清除缓存文件 (db.json) 和已生成的静态文件 (public)</td>
</tr>
<tr>
<td>hexo server（可简写为hexo s）</td>
<td>启动本地服务器</td>
</tr>
<tr>
<td>hexo deploy （可简写为hexo d）</td>
<td>部署网站，可加参数-g，部署之前预先生成静态文件</td>
</tr>
<tr>
<td>hexo version</td>
<td>显示 Hexo 版本</td>
</tr>
</tbody>
</table>
<p>&emsp;&emsp;以上就是hexo最常用的命令，文章编辑完成准备发布的时候，可直接使用以下命令，一次搞定：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">hexo g -d  <span class="comment">// 生成静态文件并部署网站</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;如果想一次使用多条命令，也可以用<code>&amp;&amp;</code>连字符来连接多条命令。比如文章编辑好想先在本地预览效果：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">hexo g &amp;&amp; hexo s  <span class="comment">// 生成静态文件后，启动本地服务器</span></span><br></pre></td></tr></table></figure>
<p><a href="https://hexo.io/zh-cn/docs/commands#generate" target="_blank" rel="noopener">更多Hexo 指令</a></p>
]]></content>
      <categories>
        <category>技术</category>
        <category>工具</category>
      </categories>
      <tags>
        <tag>Hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>Electron介绍（理论篇）</title>
    <url>/2019/electron-desc.html</url>
    <content><![CDATA[<p>&emsp;&emsp;Electron是一个能让你使用前端技术（JavaScript, HTML, CSS和Node.js）构建跨平台的桌面应用的框架。最初被GitHub开发，2013年4月11日以Atom Shell为名起步，2014年5月16日开源，2015年4月17日改名为Electron。</p>
<a id="more"></a>
<h2 id="基于Electron实现的产品"><a href="#基于Electron实现的产品" class="headerlink" title="基于Electron实现的产品"></a>基于Electron实现的产品</h2><p>&emsp;&emsp;VsCode, Atom等。</p>
<h2 id="Electron组成"><a href="#Electron组成" class="headerlink" title="Electron组成"></a>Electron组成</h2><p>&emsp;&emsp;Electron主要由Node.js、Chromium和封装好的系统API三大部分组成。</p>
<p>&emsp;&emsp;在Electron构建的程序里，Chromium负责页面渲染进程，Node.js负责程序的逻辑部分，开发中可以使用所有Node.js的原生API，也可以使用npm平台上的所有模块。使用Electron的API可以实现如通知、自定义Windows任务栏/MacOS Dock、最近文档等操作系统原生功能。</p>
<h2 id="Electron进程"><a href="#Electron进程" class="headerlink" title="Electron进程"></a>Electron进程</h2><p>&emsp;&emsp;Electron有两种进程，分别是主进程和渲染进程。</p>
<p>&emsp;&emsp;主进程只能有一个，负责调用Electron的API实现系统功能，控制应用的生命周期，创建单独的渲染进程来加载web页面等。</p>
<p>&emsp;&emsp;每个单独的web页面都运行在属于它自己的渲染进程中，是相互独立的。这意味着某一个渲染进程崩溃了，并不会影响到其它的渲染进程。渲染进程负责渲染页面和处理相应的页面交互。有一点需要注意的是，渲染进程并不能直接调用与 GUI 相关的原生 API （比如操作系统菜单、创建新窗口等），因为渲染进程等于是使用的Chromium来展示web页面，在web页面中，直接操作原生的 GUI 资源是非常危险的，而且容易造成资源泄露。如果想要在渲染进程内使用GUI操作，可以与主进程产生通讯，请求主进程调用相应的API来操作。</p>
<h2 id="Electron优缺点"><a href="#Electron优缺点" class="headerlink" title="Electron优缺点"></a>Electron优缺点</h2><h3 id="优点："><a href="#优点：" class="headerlink" title="优点："></a>优点：</h3><ul>
<li><p>快速构建跨平台（Windows、MacOs、Linux）的桌面级应用</p>
</li>
<li><p>因为使用Node.js开发，所以npm平台下的包都可以使用，提升开发效率，避免重复造轮子</p>
</li>
<li><p>有很多现成的UI框架可以使用</p>
</li>
</ul>
<h3 id="缺点："><a href="#缺点：" class="headerlink" title="缺点："></a>缺点：</h3><ul>
<li><p>安装包体积略大（打包了Chromium）</p>
</li>
<li><p>性能不如原生应用</p>
</li>
</ul>
]]></content>
      <categories>
        <category>技术</category>
        <category>工具</category>
      </categories>
      <tags>
        <tag>Electron</tag>
        <tag>前端</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript继承</title>
    <url>/2018/js-inheritance.html</url>
    <content><![CDATA[<h2 id="原型链继承"><a href="#原型链继承" class="headerlink" title="原型链继承"></a>原型链继承</h2><p>&emsp;&emsp;上篇<a href="https://blog.xiekaifei.com/2018/js-constructor.html">《JavaScript中的构造函数及原型模式》</a>的文章里提到了只要创建了一个新函数，就会为该函数创建一个<code>prototype</code>属性（指向函数的原型对象），所有的原型对象都会自动获得一个<code>constructor</code>属性，这个属性包含一个指向构造函数的指针。下面先看代码：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">One</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>.a = <span class="string">'a'</span></span><br><span class="line">  <span class="keyword">this</span>.num = [<span class="number">1</span>, <span class="number">2</span>]</span><br><span class="line">&#125;</span><br><span class="line">One.prototype.sayName = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'xiaoming'</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Two</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line">Two.prototype = <span class="keyword">new</span> One()  <span class="comment">// 直接把继承函数prototype指向的构造函数改为被继承的构造函数</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> two = <span class="keyword">new</span> Two()</span><br><span class="line">two.sayName()  <span class="comment">// 控制台打印"xiaoming"，继承了One()的方法</span></span><br><span class="line"><span class="keyword">var</span> two2 = <span class="keyword">new</span> Two()</span><br><span class="line">two2.num.push(<span class="number">3</span>)</span><br><span class="line">two2.a = <span class="string">'b'</span></span><br><span class="line"></span><br><span class="line">two2.num  <span class="comment">// 输出[1, 2, 3]</span></span><br><span class="line">two.num  <span class="comment">// 输出[1, 2, 3]</span></span><br><span class="line">two2.a  <span class="comment">// 输出b</span></span><br><span class="line">two.a  <span class="comment">// 输出a</span></span><br></pre></td></tr></table></figure>
<a id="more"></a>
<p>&emsp;&emsp;上面的代码将继承函数<code>Two()</code>的<code>prototype</code>直接修改为了构造函数<code>One()</code>的实例，继承了<code>One()</code>的方法，所以实例two在调用<code>sayName()</code>方法会直接输出xiaoming，而被继承函数<code>One()</code>的num属性也被继承了下来。但是当第二个实例two2修改num属性的时候，第一个实例two的num属性也被随之修改了，因为所有实例共享的是一个<code>num</code>属性，这就是原型链继承的缺点。当然这个缺点只是针对引用数据类型，而基本数据类型不受这个影响，这一点可以从<code>a</code>属性的修改中看出。</p>
<h2 id="借用构造函数"><a href="#借用构造函数" class="headerlink" title="借用构造函数"></a>借用构造函数</h2><p>&emsp;&emsp;直接看代码吧：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">One</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.name = name</span><br><span class="line">    <span class="keyword">this</span>.sayName = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="keyword">this</span>.name)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">One.prototype.sayHi = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'Hi!'</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Two</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    One.call(<span class="keyword">this</span>, <span class="string">'xiaoming'</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> two = <span class="keyword">new</span> Two(<span class="string">'xiaoming'</span>)</span><br><span class="line">two.sayName()  <span class="comment">// 控制台打印"xiaoming"</span></span><br><span class="line">two.sayHi()  <span class="comment">// 报错</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;上面的继承函数<code>Two()</code>使用了<code>call()</code>方法，使用了被继承函数<code>One()</code>的方法替换了自己的方法，所以实例two在调用<code>sayName()</code>的时候成功输出”xiaoming”，而且在调用的方法的时候还可以传参数进去，这里也可以使用<code>apply()</code>方法，与<code>call()</code>不同的是<code>apply()</code>的第二个参数是一个数组，<code>call()</code>可以只传入第一个参数，也可以在第一个参数后依次传入多个参数。</p>
<p>&emsp;&emsp;使用这种方法也实现了继承，但是这种方法也有缺点，就是继承函数只能继承构造函数内的属性和方法，原型内的属性和方法对于继承函数来说是不可见的。这就是为什么最后调用<code>two.sayHi()</code>方法会报错了。</p>
<h2 id="组合继承"><a href="#组合继承" class="headerlink" title="组合继承"></a>组合继承</h2><p>&emsp;&emsp;《JavaScript高级程序设计》一书中说组合继承有时也被叫做伪经典继承，指的是将原型链继承和借用构造函数的技术组合到一块，从而发挥二者之长的一种继承模式。这样既通过在原型定义方法实现了函数的复用，又能保证每个实例都有它自己的属性。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">One</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.name = name</span><br><span class="line">    <span class="keyword">this</span>.num = [<span class="number">1</span>, <span class="number">2</span>]</span><br><span class="line">&#125;</span><br><span class="line">One.prototype.sayHi = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'Hi!'</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Two</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">    <span class="comment">// 继承属性</span></span><br><span class="line">    One.call(<span class="keyword">this</span>, name)</span><br><span class="line">&#125;</span><br><span class="line">Two.prototype = <span class="keyword">new</span> One()  <span class="comment">// 继承方法</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> two = <span class="keyword">new</span> Two(<span class="string">'xkf'</span>)</span><br><span class="line"><span class="keyword">var</span> two2 = <span class="keyword">new</span> Two(<span class="string">'xkf'</span>)</span><br><span class="line">two.name  <span class="comment">// 输出'xkf'</span></span><br><span class="line">two2.num.push(<span class="number">3</span>)</span><br><span class="line">two.num  <span class="comment">// 输出[1, 2]</span></span><br><span class="line">two2.num  <span class="comment">// 输出[1, 2, 3]</span></span><br><span class="line">two.sayHi === two2.sayHi  <span class="comment">// true</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;这种继承方法就是将前两种继承方法组合到了一起，集合了两种方法的优点，既能保证多个被继承实例拥有各自的属性或方法（如上面代码中的num属性），又能共享原型中的属性或方法（上面的<code>sayHi()</code>方法返回true说明两个实例共享一个方法），是比较常用的一种继承方法。</p>
<h2 id="寄生组合式继承"><a href="#寄生组合式继承" class="headerlink" title="寄生组合式继承"></a>寄生组合式继承</h2><p>&emsp;&emsp;在ES5中，尽管组合继承看起来很完美，但是也有缺点。上面<strong>组合继承</strong>方法内示例代码的第10行<code>One.call(this, name)</code>和第12行<code>Two.prototype = new One()</code>，我们分开来说。第12行先是将Two.prototype指向了One的实例，这时Two的prototype得到了<code>num</code>数组（name属性的值需要传入，不考虑第10行的话这个属性并没有获取到），这是第一次调用构造函数One。第10行采用了<strong>构造函数继承</strong>的方法，这个时候<code>num</code>数组又被创建了一次，这第二次调用构造函数One创建的<code>num</code>数组屏蔽了上一次原型内的<code>num</code>数组，所以这两次调用导致了数组<code>num</code>被创建了两次，造成了不必要的浪费。</p>
<p>&emsp;&emsp;所以我们可以使用<strong>寄生组合式继承</strong>的方法来实现较完美的继承。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">One</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>.name = name</span><br><span class="line">  <span class="keyword">this</span>.num = [<span class="number">1</span>, <span class="number">2</span>]</span><br><span class="line">&#125;</span><br><span class="line">One.prototype.sayHi = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'Hi!'</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Two</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// 继承属性</span></span><br><span class="line">  One.call(<span class="keyword">this</span>, name)</span><br><span class="line">&#125;</span><br><span class="line">Two.prototype = <span class="built_in">Object</span>.create(One.prototype)  <span class="comment">// 调用Object.create()方法来修改原型</span></span><br><span class="line">Two.prototype.constructor = Two  <span class="comment">// 将构造函数指回自己</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> two = <span class="keyword">new</span> Two(<span class="string">'xkf'</span>)</span><br><span class="line"><span class="keyword">var</span> two2 = <span class="keyword">new</span> Two(<span class="string">'xkf'</span>)</span><br><span class="line">two.name  <span class="comment">// 输出'xkf'</span></span><br><span class="line">two2.num.push(<span class="number">3</span>)</span><br><span class="line">two.num  <span class="comment">// 输出[1, 2]</span></span><br><span class="line">two2.num  <span class="comment">// 输出[1, 2, 3]</span></span><br><span class="line">two.sayHi === two2.sayHi  <span class="comment">// true</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;我们用了一个<a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Object/create" target="_blank" rel="noopener"><code>Object.create()</code></a>方法，这个方法会创建一个新对象，并使用第一个参数提供的现有对象来作为新创建对象的<code>__proto__</code>，并将这个新对象作为返回值。具体实现如下（本方法有两个参数，下面展示的只是第一个参数的实现，摘自《JavaScript高级程序设计》6.3.4）：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">object</span>(<span class="params">o</span>) </span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">F</span>(<span class="params"></span>) </span>&#123; &#125;</span><br><span class="line">  F.prototype = o</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> F()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;我们将<code>One.prototype</code>作为<code>Object.create()</code>的第一个参数执行后赋值给了<code>Two.prototype</code>，这时<code>num</code>数组自然可以得到继承。下面一行我们重新把<code>Two</code>的构造函数指回了它自己，使得后面传入的<code>name</code>属性也可以被创建。</p>
<p>&emsp;&emsp;所以在ES5中，寄生组合式继承算是一个相对完美的继承方法。</p>
<h2 id="class继承"><a href="#class继承" class="headerlink" title="class继承"></a>class继承</h2><p>&emsp;&emsp;ES6中新引入了<code>class</code>关键字，并且加入了class继承。这种方法通过<code>extends</code>来实现继承，减少了使用继承的代码量，使代码变的更加清晰易懂。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Person</span> </span>&#123;</span><br><span class="line">    <span class="keyword">constructor</span>(name, age) &#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name</span><br><span class="line">        <span class="keyword">this</span>.age = age</span><br><span class="line">    &#125;</span><br><span class="line">    sayName() &#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="keyword">this</span>.name)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Xm</span> <span class="keyword">extends</span> <span class="title">Person</span> </span>&#123;</span><br><span class="line">    <span class="keyword">constructor</span>(name, age, job) &#123;</span><br><span class="line">        <span class="keyword">super</span>(name, age)  <span class="comment">// 使用"super"来调用父类Person中的name和age，且必须在this之前</span></span><br><span class="line">        <span class="keyword">this</span>.job = job</span><br><span class="line">    &#125;</span><br><span class="line">    sayHi() &#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="string">'Hi!'</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> xm = <span class="keyword">new</span> Xm(<span class="string">'xiaoming'</span>, <span class="number">24</span>, <span class="string">'Engineer'</span>)</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;上面的代码先用<code>class</code>关键字定义了<code>Person</code>，然后Xm用<code>intends</code>继承了<code>Person</code>。这里需要注意的是，子类在继承父类的时候必须先在自己的构造函数（constructor）里调用<code>super</code>方法，而且要写在<code>this</code>之前，否则会报错。这是因为子类实例的构建，基于父类实例，只有<code>super</code>方法才能调用父类实例。</p>
<p>&emsp;&emsp;如果子类没有定义<code>constructor</code>方法，那这个方法会被默认添加。也就是说，不管有没有显式定义，任何子类都有<code>constructor</code>方法。</p>
]]></content>
      <categories>
        <category>技术</category>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>javascript</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript创建ajax异步请求</title>
    <url>/2019/js-ajax.html</url>
    <content><![CDATA[<p>&emsp;&emsp;利用ajax，可以创建异步请求，从而实现在不影响整个页面的情况下，局部更新网页某处的数据。</p>
<p>&emsp;&emsp;异步请求可以应用在多个地方，最常见的就是我们在注册某个网站账号的时候，输入要注册的用户名，当鼠标焦点离开用户名输入表单的时候，如果输入的用户名已经存在于网站内，右侧会给出有相同用户名的提示。试想如果没有该功能，用户就必须在所有信息都填写完毕的情况下，点击”提交”按钮，才能知道用户名是否可以使用，大大降低效率。</p>
<a id="more"></a>
<h2 id="创建XMLHttpReques对象"><a href="#创建XMLHttpReques对象" class="headerlink" title="创建XMLHttpReques对象"></a>创建XMLHttpReques对象</h2><p>&emsp;&emsp;先用js来写，第一步用构造函数初始化一个<code>XMLHttpReques</code>对象。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> request = <span class="keyword">new</span> XMLHttpRequest()</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;目前新版本的浏览器基本都支持<code>XMLHttpReques</code>对象，只有像IE5/IE6这种老旧的浏览器不支持，需要改用<code>ActiveX</code>对象。当然你也可以在使用<code>XMLHttpReques</code>前来做一个判断，以保证更强的兼容性。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> request</span><br><span class="line"><span class="keyword">if</span> (<span class="built_in">window</span>.XMLHttpRequest) &#123;</span><br><span class="line">  request = <span class="keyword">new</span> XMLHttpRequest()</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">  <span class="comment">// IE5/IE6下使用</span></span><br><span class="line">  request = <span class="keyword">new</span> ActiveObject(<span class="string">'Microsoft.XMLHTTP'</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="使用onreadystatechange监听是否加载完成"><a href="#使用onreadystatechange监听是否加载完成" class="headerlink" title="使用onreadystatechange监听是否加载完成"></a>使用onreadystatechange监听是否加载完成</h2><p>&emsp;&emsp;创建完成后，使用<code>onreadystatechange</code>来监听<code>readyState</code>和<code>status</code>属性的改变。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">request.onreadystatechange = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (request.readyState === <span class="number">4</span> &amp;&amp; request.status === <span class="number">200</span>) &#123;</span><br><span class="line">    <span class="comment">// 需要执行的代码</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;首先来说一下<code>onreadystatechange</code>。这个方法会在<code>readyState</code>属性发生改变时触发<code>readystatechange</code>事件的时候被调用。</p>
<p>&emsp;&emsp;接着是<code>readyState</code>和<code>status</code>属性。</p>
<h3 id="readyState"><a href="#readyState" class="headerlink" title="readyState:"></a>readyState:</h3><blockquote>
<p><strong>XMLHttpRequest.readyState</strong> 属性返回一个 XMLHttpRequest  代理当前所处的状态。一个 XHR 代理总是处于下列状态中的一个：</p>
<table>
<thead>
<tr>
<th>值</th>
<th>状态</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>0</code></td>
<td><code>UNSENT</code></td>
<td>代理被创建，但尚未调用 open() 方法。</td>
</tr>
<tr>
<td><code>1</code></td>
<td><code>OPENED</code></td>
<td><code>open()</code> 方法已经被调用。</td>
</tr>
<tr>
<td><code>2</code></td>
<td><code>HEADERS_RECEIVED</code></td>
<td><code>send()</code> 方法已经被调用，并且头部和状态已经可获得。</td>
</tr>
<tr>
<td><code>3</code></td>
<td><code>LOADING</code></td>
<td>下载中； <code>responseText</code> 属性已经包含部分数据。</td>
</tr>
<tr>
<td><code>4</code></td>
<td><code>DONE</code></td>
<td>下载操作已完成。</td>
</tr>
</tbody>
</table>
<p>引用自MDN <a href="https://developer.mozilla.org/zh-CN/docs/Web/API/XMLHttpRequest/readyState" target="_blank" rel="noopener">XMLHttp​Request​.ready​State</a></p>
</blockquote>
<h3 id="status"><a href="#status" class="headerlink" title="status:"></a>status:</h3><p>&emsp;&emsp;<code>status</code>属性则返回一个响应中的数字状态码，比如”200”代表一个成功的请求。</p>
<h2 id="向服务器发送请求"><a href="#向服务器发送请求" class="headerlink" title="向服务器发送请求"></a>向服务器发送请求</h2><p>&emsp;&emsp;首先用<code>open()</code>方法来初始化一个请求。</p>
<h3 id="open"><a href="#open" class="headerlink" title="open()"></a>open()</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">request.open(<span class="string">'GET'</span>, <span class="string">'./text.json'</span>)</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;<code>open()</code>有三个参数，第一个指定要使用的http方法，如<code>GET</code>方法或者<code>POST</code>方法，第二个参数指定要向其发送请求的url，第三个参数是一个布尔值，表示是否用异步执行操作，默认为ture，表示使用异步执行。</p>
<h3 id="send"><a href="#send" class="headerlink" title="send()"></a>send()</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">request.send()</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;<code>send()</code>表示发送请求。</p>
<h2 id="实际使用示例"><a href="#实际使用示例" class="headerlink" title="实际使用示例"></a>实际使用示例</h2><p>比如同目录下有一个名为”text.json”的json文件：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="attr">"student"</span>: [</span><br><span class="line">    &#123;<span class="attr">"name"</span>: <span class="string">"小明"</span>, <span class="attr">"age"</span>: <span class="number">12</span>&#125;,</span><br><span class="line">    &#123;<span class="attr">"name"</span>: <span class="string">"小红"</span>, <span class="attr">"age"</span>: <span class="number">13</span>&#125;</span><br><span class="line">  ]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>要实现点击按钮，获取小明的年龄，html代码如下：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">button</span> <span class="attr">type</span>=<span class="string">"button"</span> <span class="attr">id</span>=<span class="string">"btn"</span>&gt;</span>click me<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span>&gt;</span></span><br><span class="line">    小明的年龄为：<span class="tag">&lt;<span class="name">span</span> <span class="attr">id</span>=<span class="string">"age"</span> <span class="attr">style</span>=<span class="string">"color: red;"</span>&gt;</span><span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>js代码如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> btn = <span class="built_in">document</span>.getElementById(<span class="string">'btn'</span>)</span><br><span class="line"><span class="keyword">var</span> age = <span class="built_in">document</span>.getElementById(<span class="string">'age'</span>)</span><br><span class="line"><span class="keyword">var</span> request = <span class="keyword">new</span> XMLHttpRequest()</span><br><span class="line"><span class="keyword">var</span> student</span><br><span class="line">request.open(<span class="string">'GET'</span>, <span class="string">'./text.json'</span>)</span><br><span class="line">request.onreadystatechange = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (request.readyState === <span class="number">4</span> &amp;&amp; request.status === <span class="number">200</span>) &#123;</span><br><span class="line">    <span class="comment">// 获取json文件的值，并转换为js对象</span></span><br><span class="line">    student = <span class="built_in">JSON</span>.parse(request.responseText).student</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">request.send()</span><br><span class="line"></span><br><span class="line">btn.onclick = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  age.innerHTML = student[<span class="number">0</span>].age</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;上面的代码还用到了<code>responseText()</code>和<code>JSON.parse()</code>两个方法。</p>
<h3 id="responseText"><a href="#responseText" class="headerlink" title="responseText()"></a>responseText()</h3><p>&emsp;&emsp;该方法返回从服务器接收到的字符串。</p>
<h3 id="JSON-parse"><a href="#JSON-parse" class="headerlink" title="JSON.parse()"></a>JSON.parse()</h3><p>&emsp;&emsp;该方法将一个json字符串转换为js对象。还可以添加一个转换结果函数，为该方法的第二个参数，将为对象的每个成员调用该方法。</p>
<h2 id="跨域"><a href="#跨域" class="headerlink" title="跨域"></a>跨域</h2><p>&emsp;&emsp;异步请求json文件，该文件直接使用的话必须与调用文件为同协议、同地址、同端口才能正常实现功能。如跨域使用需要使用其他方法来实现，本文没有提到跨域相关的内容，以后有机会再来谈一谈跨域。</p>
]]></content>
      <categories>
        <category>技术</category>
        <category>ajax</category>
      </categories>
      <tags>
        <tag>javascript</tag>
        <tag>ajax</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript中的__proto__与prototype</title>
    <url>/2019/js-prototype.html</url>
    <content><![CDATA[<p>&emsp;&emsp;最近接了几个面试，个别出色的面试官在面试技术部分的时候总是很考察被试者的原生js能力，而业务逻辑写多了就会逐渐淡忘掉最基础同时也是最重要的js知识。所以，工作的同时多复习原生js知识是非常重要的。今天就来聊一下js中__proto__和prototype。</p>
<a id="more"></a>
<h2 id="prototype"><a href="#prototype" class="headerlink" title="prototype"></a>prototype</h2><p>&emsp;&emsp;prototype属性即js的原型属性，是为了给所有实例添加共享属性和共享方法的属性。使用方法可见<a href="https://blog.xiekaifei.com/2018/js-constructor.html">这篇文章</a>中的“原型模式”部分。</p>
<p>&emsp;&emsp;prototype这个属性指向一个包含所有共享属性和共享方法的对象，我们称之为原型对象。同时这个原型对象也有一个叫做”constructor”的属性，这个属性指回的是原构造函数。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Person</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>.name = <span class="string">'xkf'</span></span><br><span class="line">  <span class="keyword">this</span>.age = <span class="number">18</span></span><br><span class="line">&#125;</span><br><span class="line">Person.prototype.sayName = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="keyword">this</span>.name)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(Person.prototype)  <span class="comment">// 会打印出sayName, constructor, __proto__</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;上面的代码先是创建了一个名为”Person”的构造函数，然后给这个方法的原型属性（prototype）内添加了一个所有实例共享的”sayName”方法，最后在控制台打印出了该构造函数的”prototype”属性。可以看到，prototype内包含之前添加的sayName方法，接着有constructor属性，这个属性指回原构造函数也就是它自己”Person”。最后还有一个__proto__。</p>
<h2 id="proto"><a href="#proto" class="headerlink" title="__proto__"></a>__proto__</h2><p>&emsp;&emsp;js的每个实例对象中会有__proto__这样一个私有属性，有些人把这个属性称之为“隐式原型”，这个属性指向构造该对象的构造函数的原型属性（prototype）。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Person</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>.name = <span class="string">'xkf'</span></span><br><span class="line">  <span class="keyword">this</span>.age = <span class="number">18</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> p = <span class="keyword">new</span> Person()</span><br><span class="line"><span class="built_in">console</span>.log(p.__proto__ === Person.prototype)  <span class="comment">// true</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;上面的代码先是创建了一个名为”Person”的构造函数，然后基于”Person”创建了它的实例”p”。这个时候如果直接打印p的话会看到，p里面除了有继承自构造函数Person中的属性”name”和”age”之外，还有一个私有属性（隐式原型）__proto__，而它指向的就是构造该对象的构造函数（Person）的原型，所以两者在最后比较的时候会返回”true”。</p>
]]></content>
      <categories>
        <category>技术</category>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>javascript</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript的赋值、浅拷贝和深拷贝</title>
    <url>/2019/js-copy.html</url>
    <content><![CDATA[<p>&emsp;&emsp;js里的数据类型分为基本数据类型和引用数据类型，基本数据类型有<code>undefined</code>, <code>boolean</code>, <code>number</code>, <code>string</code>, <code>null</code>, <code>Symbol</code>(ES6中新增)，而引用数据类型就是<code>Object</code>。两种类型在赋值上面是有一些区别的。</p>
<a id="more"></a>
<h2 id="赋值"><a href="#赋值" class="headerlink" title="赋值"></a>赋值</h2><h3 id="基本数据类型"><a href="#基本数据类型" class="headerlink" title="基本数据类型"></a>基本数据类型</h3><p>&emsp;&emsp;基本数据类型的赋值操作是直接拷贝一份原数据的副本，赋值后改变原数据的值并不会影响到副本数据的值，反之也不会相互影响。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">1</span></span><br><span class="line"><span class="keyword">var</span> b = a</span><br><span class="line">a = <span class="number">2</span></span><br><span class="line"><span class="built_in">console</span>.log(b)  <span class="comment">// 改变原数据a的值，并不会影响副本数据b的值，b输出1</span></span><br></pre></td></tr></table></figure>
<h3 id="引用数据类型"><a href="#引用数据类型" class="headerlink" title="引用数据类型"></a>引用数据类型</h3><p>&emsp;&emsp;而引用数据就不同了，赋值操作后改变原数据和副本数据任何一个数据的值都会影响另一个数据的值。这是因为在将一个引用数据的值赋值给副本数据后，副本数据保存的是和原引用数据相同（以下简称原数据）的指针，该指针和原数据指向同样的内存地址。其中任何一方修改这个指针的值，内存地址对应存储空间上的值发生改变后，另一方的指针指向的位置不变，所以对应指向的值也就发生了相同的改变。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = &#123;</span><br><span class="line">  num: <span class="number">10</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> b = a</span><br><span class="line">b.num = <span class="number">20</span></span><br><span class="line"><span class="built_in">console</span>.log(a.num)  <span class="comment">// 改变副本数据b.num的值，会影响到原数据a.num的值，反之也是，a.num输出20</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;因为引用数据的特性，这让我们在想进行拷贝引用数据操作时发生了问题。如果想拷贝引用数据，修改副本数据时不影响原数据的话，就要用到拷贝方法。拷贝方法又分为浅拷贝和深拷贝。</p>
<h2 id="浅拷贝"><a href="#浅拷贝" class="headerlink" title="浅拷贝"></a>浅拷贝</h2><h3 id="遍历实现浅拷贝"><a href="#遍历实现浅拷贝" class="headerlink" title="遍历实现浅拷贝"></a>遍历实现浅拷贝</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 我们先来创建一个a对象</span></span><br><span class="line"><span class="keyword">var</span> a = &#123;</span><br><span class="line">  arr: [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 再封装一个浅拷贝函数，target是要复制的副本对象，source是原对象</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">shallowCopy</span>(<span class="params">target, source</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> key <span class="keyword">in</span> source) &#123;</span><br><span class="line">    target[key] = source[key]</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> b = &#123;&#125;  <span class="comment">// 创建一个空的b对象</span></span><br><span class="line">shallowCopy(b, a)  <span class="comment">// 用封装好的浅拷贝函数把原对象a的值拷贝给b对象</span></span><br><span class="line">a.arr = [<span class="string">'一'</span>, <span class="string">'二'</span>]  <span class="comment">// 修改原对象a.arr的值</span></span><br><span class="line"><span class="built_in">console</span>.log(b)  <span class="comment">// 输出[1, 2, 3]。这时修改原数据a的值并不影响副本数据b的值</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;但是浅拷贝方法也有一个问题，继续看：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 重新创建一个c对象</span></span><br><span class="line"><span class="keyword">var</span> c = &#123;</span><br><span class="line">  arr: [<span class="number">1</span>, [<span class="number">2</span>, <span class="number">3</span>]]</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> d = &#123;&#125;  <span class="comment">// 重新创建一个空的d对象</span></span><br><span class="line">shallowCopy(d, c)  <span class="comment">// 用刚刚封装好的浅拷贝函数把原对象c的值拷贝给d对象</span></span><br><span class="line">c.arr[<span class="number">1</span>] = [<span class="string">'二'</span>, <span class="string">'三'</span>]  <span class="comment">// 修改原对象c.arr[1]的值，这个值是一个数组，也是一个引用数据类型的值</span></span><br><span class="line"><span class="built_in">console</span>.log(d)  <span class="comment">// 输出[1, ["二", "三"]]</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;这时修改原数据c的值，副本数据d也一起被修改了。这是什么原因呢？原来是因为浅拷贝操作只能复制一层对象的属性，并不包括对象里的引用数据类型的值，所以当修改原数据c里的数组的值后，浅拷贝的副本数据d的数组值也就一起被改变了。如果要完全独立拷贝副本数据，就需要用到深拷贝的方法了。</p>
<h3 id="ES6浅拷贝数组方法"><a href="#ES6浅拷贝数组方法" class="headerlink" title="ES6浅拷贝数组方法"></a>ES6浅拷贝数组方法</h3><p>&emsp;&emsp;ES6新增了扩展运算符<code>...</code>，可以实现数组的浅拷贝。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 创建原始数组</span></span><br><span class="line"><span class="keyword">var</span> a = [<span class="number">1</span>, <span class="number">2</span>, [<span class="number">3</span>, <span class="number">4</span>], <span class="number">5</span>]</span><br><span class="line"><span class="keyword">var</span> b = [...a]</span><br><span class="line">a[<span class="number">1</span>] = <span class="number">100</span></span><br><span class="line"><span class="built_in">console</span>.log(b)  <span class="comment">// 输出[1, 2, [3, 4], 5]。副本数据b并没有随着a一起改变。（仅限于浅拷贝）</span></span><br></pre></td></tr></table></figure>
<h2 id="深拷贝"><a href="#深拷贝" class="headerlink" title="深拷贝"></a>深拷贝</h2><h3 id="使用JSON序列化实现深拷贝"><a href="#使用JSON序列化实现深拷贝" class="headerlink" title="使用JSON序列化实现深拷贝"></a>使用JSON序列化实现深拷贝</h3><p>&emsp;&emsp;深拷贝的一种方法是利用<code>JSON.stringify()</code>方法，先将原对象转化为json格式，再用<code>JSON.parse()</code>方法将转化后的json数据转回js对象的方式，进行深拷贝。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 还是重新创建一个a对象</span></span><br><span class="line"><span class="keyword">var</span> a = &#123;</span><br><span class="line">  arr: [<span class="number">1</span>, [<span class="number">2</span>, <span class="number">3</span>]]</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 这时来封装一个深拷贝函数</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">deepCopy</span>(<span class="params">source</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="built_in">JSON</span>.parse(<span class="built_in">JSON</span>.stringify(source))</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> b = deepCopy(a)  <span class="comment">// 将深拷贝后的值赋给b对象（副本对象）</span></span><br><span class="line">a.arr[<span class="number">1</span>] = [<span class="string">'二'</span>, <span class="string">'三'</span>]  <span class="comment">// 修改原数据内层引用数据类型的值</span></span><br><span class="line"><span class="built_in">console</span>.log(b)  <span class="comment">// 输出[1, [2, 3]]。并不会影响拷贝后副本对象b</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;这样，我们就可以用深拷贝的方法拷贝引用数据类型的值，随意修改原数据或副本数据的其中一个，而不用担心另一个也会被随之修改了。</p>
<h3 id="递归实现对象的深拷贝"><a href="#递归实现对象的深拷贝" class="headerlink" title="递归实现对象的深拷贝"></a>递归实现对象的深拷贝</h3><p>&emsp;&emsp;还有一种递归实现深拷贝的方法。解决思路是先用<code>for...in...</code>的方法遍历原对象，检测每次遍历的值是否为对象，是的话再次调用自身递归循环遍历，不是的话直接添加。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 先创建原始a对象</span></span><br><span class="line"><span class="keyword">var</span> a = &#123;</span><br><span class="line">  person: &#123;</span><br><span class="line">    name: <span class="string">'test_a'</span>,</span><br><span class="line">    age: <span class="number">10</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 封装一个验证是否为对象的函数</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">isObject</span>(<span class="params">obj</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="keyword">typeof</span> obj === <span class="string">'object'</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 封装递归对象深拷贝函数</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">deepCopy</span>(<span class="params">obj</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> copyObj = &#123;&#125;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> key <span class="keyword">in</span> obj) &#123;</span><br><span class="line">    copyObj[key] = isObject(obj[key]) ? deepCopy(obj[key]) : obj[key]  <span class="comment">// 当遍历的数据为对象时，调用自身递归继续遍历子对象</span></span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> copyObj</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> b = deepCopy(a)</span><br><span class="line">a.person.age = <span class="number">20</span>  <span class="comment">// 修改原对象a.person.age的值</span></span><br><span class="line"><span class="built_in">console</span>.log(b)  <span class="comment">//输出&#123; person: &#123; name: "test_a"; age: 10 &#125; &#125;。拷贝后的子对象没有随之改变</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;这种方法只针对内部全是对象的引用数据类型。如果还想加上数组，需要再加上一层验证。</p>
<p>&emsp;&emsp;需要注意的是两种方法都不完美，都不支持值为<code>Function</code>或<code>正则</code>等格式的引用数据类型，需要寻找其它的解决方案。要想实现完美的深拷贝还需要考虑怎么处理原型等问题，一般在实际应用当中，基本上是没有全部需要深拷贝的情况的。上面说的两种方法在一般在业务中基本上是足够使用的。</p>
]]></content>
      <categories>
        <category>技术</category>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>javascript</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript中的构造函数及原型模式</title>
    <url>/2018/js-constructor.html</url>
    <content><![CDATA[<p>&emsp;&emsp;今天详细了解了一下JavaScript中构造函数和原型模式的相关知识，做了一个总结，以便日后做一个参考。</p>
<h2 id="构造函数"><a href="#构造函数" class="headerlink" title="构造函数"></a>构造函数</h2><p>&emsp;&emsp;构造函数可以用来创建特定类型的对象。先来看一个构造函数：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Person</span>(<span class="params">name, age</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.name = name</span><br><span class="line">    <span class="keyword">this</span>.age = age</span><br><span class="line">    <span class="keyword">this</span>.sayName = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="keyword">this</span>.name)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> xm = <span class="keyword">new</span> Person(<span class="string">"小明"</span>, <span class="number">20</span>) <span class="comment">// 这句代码干了四件事</span></span><br><span class="line"><span class="comment">// 1.创建了一个新对象 2.将Person的作用域赋值给新对象 3.执行Person内的代码 4.将这个新对象返回给xm</span></span><br><span class="line">xm.name  <span class="comment">// 小明</span></span><br><span class="line">xm.age  <span class="comment">// 20</span></span><br><span class="line">xm.sayName()  <span class="comment">// 控制台打印小明的名字</span></span><br></pre></td></tr></table></figure>
<a id="more"></a>
<p>&emsp;&emsp;声明构造函数按照惯例通常以一个大写字母来开头，构造函数的新实例用<code>new</code>操作符创建。任何函数用new操作符来调用就是构造函数，而构造函数不通过<code>new</code>操作符调用也就和普通函数没什么两样。</p>
<p>&emsp;&emsp;使用构造函数的缺点是每个方法都需要在每个实例上重新创建一遍。看下面代码：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> xf = <span class="keyword">new</span> Person(<span class="string">"小芳"</span>, <span class="number">20</span>)</span><br><span class="line"><span class="built_in">console</span>.log(xm.sayName === xf.sayName)  <span class="comment">// false</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;我在这里新建了一个名为xf的实例，这时xm和xf都有一个<code>sayName()</code>方法，但这两个方法如果去比较的话却是不一样的。这就说明两个方法是独立的，每个方法都会为其分配一定的内存用来存放，这样显然是浪费资源的。这时就需要提到另一种方法，原型模式。</p>
<h2 id="原型模式"><a href="#原型模式" class="headerlink" title="原型模式"></a>原型模式</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 构造函数</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Person</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.name = name</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 原型模式</span></span><br><span class="line">Person.prototype.age = <span class="number">20</span></span><br><span class="line">Person.sayName = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span>.name)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> xm = <span class="keyword">new</span> Person(<span class="string">"小明"</span>)</span><br><span class="line"><span class="keyword">var</span> xf = <span class="keyword">new</span> Person(<span class="string">"小芳"</span>)</span><br><span class="line"><span class="built_in">console</span>.log(xm.sayName === xf.sayName)  <span class="comment">// true</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;上面我使用了构造函数和原型模式混合的方法，其中<code>name</code>属性是构造函数创建的，<code>age</code>属性和<code>sayName()</code>方法是用原型模式（prototype属性）创建的。原型模式创建的两个方法均为所有实例共享，所以在最后比较两个实例的<code>sayName()</code>方法时返回了true，表示两者是调用的同一个<code>sayName()</code>方法。</p>
<p>&emsp;&emsp;只要创建了一个新函数，就会为该函数创建一个<strong>prototype</strong>属性，这个属性指向函数的原型对象。在默认情况下，所有的原型对象都会自动获得一个<strong>constructor</strong>(构造函数)属性，这个属性包含一个指向prototype属性所在函数的指针，也就是指向构造函数。在上面的例子里<code>Person.prototype.constructor</code>指向构造函数<code>Person</code>。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">xf.age = <span class="number">25</span>  <span class="comment">// 重写了小芳的年龄</span></span><br><span class="line">xf.age  <span class="comment">// 25</span></span><br><span class="line">xm.age  <span class="comment">// 20</span></span><br><span class="line"><span class="comment">/* 重写已经存在于原型的值后，用该实例访问该属性会返回重写后的值，这种情况会把原型中的该值屏蔽掉，但是不会影响其他实例调用原型中的属性。</span></span><br><span class="line"><span class="comment">如果需要删除实例属性，可以使用"delete"操作符完全删除实例属性。*/</span></span><br><span class="line"><span class="keyword">delete</span> xf.age</span><br><span class="line">xf.age  <span class="comment">// 20</span></span><br></pre></td></tr></table></figure>
<h2 id="原型链"><a href="#原型链" class="headerlink" title="原型链"></a>原型链</h2><p>&emsp;&emsp;也就是说，当访问实例的属性或方法时，首先会按照名称查找实例中有没有存在的属性或方法，有的话则屏蔽原型内的同名属性或方法（原型里有的话），直接调用实例中的。如果没有，则向上查找该实例的原型，原型里有的话就调用，都没有的话就返回<code>undefined</code>。这个步骤是沿着原型链查找的，也就涉及到了原型链的概念。创建了自定义的构造函数之后，其原型对象默认只会取得<code>constructor</code>属性，至于其他方法，则都是从<code>Object</code>继承而来的。<br>&emsp;&emsp;这么来理解的话，刚刚创建的xm实例的原型链是：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">xm → Person.prototype → Object.prototype → null</span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>技术</category>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>javascript</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript里的Promise</title>
    <url>/2019/js-promise.html</url>
    <content><![CDATA[<p>&emsp;&emsp;JavaScript在ES6版本中新增了<code>Promise</code>对象。</p>
<h2 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h2><blockquote>
<p><code>Promise</code> 对象是一个代理对象（代理一个值），被代理的值在Promise对象创建时可能是未知的。它允许你为异步操作的成功和失败分别绑定相应的处理方法（handlers）。</p>
</blockquote>
<a id="more"></a>
<p>以上引用自MDN中对<a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Promise" target="_blank" rel="noopener">Promise</a>的介绍。</p>
<p>&emsp;&emsp;<code>Promise</code>其实就是一个对异步操作结果的封装。一个<code>Promise</code>有三种状态：</p>
<ul>
<li>Pending: 初始状态，既不是成功，也不是失败状态。</li>
<li>Fulfilled: 表示操作成功完成。</li>
<li>rejected: 表示操作失败。</li>
</ul>
<p>&emsp;&emsp;<code>Promise</code>翻译为保证，承诺的意思。这意味着一个<code>Promise</code>一旦有了结果，就不会再发生改变。Pending是初始状态，此时表明还没有获取到结果。一旦状态从<code>Pending</code>改变为<code>Fulfilled</code>或<code>rejected</code>后，状态便不会再发生改变。</p>
<p>&emsp;&emsp;在之前的ES5时代，我们通常会用回调函数来处理异步操作的结果。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">successCallback</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'success!'</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">failCallback</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'fail!'</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">test</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="literal">true</span>) &#123;  <span class="comment">// 异步请求的结果</span></span><br><span class="line">    successCallback()</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    failCallback()</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">test()</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;上面的代码先是封装了两个回调函数，分别是成功和失败。然后在test函数内执行异步请求（忽略），根据异步请求的结果来执行对应的回调函数。如果在只有一层异步请求的时候，代码结构还算清晰。但实际业务中，往往会嵌套多层异步请求，层数一多，还用这种写法层层嵌套的话，代码会变得难以阅读和维护，也就是经典的回调地狱模式。所以这种情况就可以使用ES6新增的<code>Promise</code>方法。</p>
<p>&emsp;&emsp;<code>Promise</code>对象使用<strong>new</strong>操作符创建，接收一个函数作为参数，该函数又接收<strong>resolve</strong>和<strong>reject</strong>两个函数，这两个函数是ES6帮我们封装好的，我们直接调用就可以了。<code>resolve</code>表示成功时执行的函数，而<code>reject</code>表示失败时执行的函数。两个函数都可以传参，后期在.then里接收。</p>
<h2 id="Promise-then"><a href="#Promise-then" class="headerlink" title="Promise.then()"></a>Promise.then()</h2><p>&emsp;&emsp;<code>.then()</code>方法用于<code>Promise</code>实例的状态改变时执行的回调函数。它接收两个参数，分别是状态成功时和状态失败时执行的回调函数,同时两个函数内可以传递参数供回调函数使用。</p>
<p>&emsp;&emsp;下面我们来创建一个<code>Promise</code>实例并用.then()方法执行相应的回调函数。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> a = <span class="number">3</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">p</span>(<span class="params">value</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="literal">true</span>) &#123;  <span class="comment">// 如果异步操作成功</span></span><br><span class="line">      resolve(value)</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">'success!'</span>)</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;  <span class="comment">// 异步操作失败</span></span><br><span class="line">      reject(value)</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">'fail!'</span>)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">p(a).then(<span class="function">(<span class="params">a</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(a)</span><br><span class="line">&#125;, (a) =&gt; &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(a)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;上面的代码我们首先声明了一个属性a，紧接着在p函数里封装了一个<code>Promise</code>对象并返回。里面假设进行了一个异步请求。如果异步请求执行成功，执行<code>resolve</code>函数，并把a当作参数传递进去。如果异步请求执行失败，执行<code>reject</code>函数，同时也把参数a传递进去。之后我们执行了p函数，将参数a传递进去，并在.then()内写了两个箭头函数用于执行成功和失败的回调。这时运行代码控制台会输出：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">success!</span><br><span class="line"><span class="number">3</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;运行到这里说明走了resolve函数，并且获取到了传进来的参数a。如果此时将p函数内的异步操作结果修改为<code>false</code>，就会执行<code>reject</code>函数内的代码了。这里需要注意的是，success虽然写在<code>resolve</code>的后面，但是先于<code>resolve</code>执行的。因为在<code>Promise</code>对象里，同步代码总是先于异步代码执行。</p>
<p>&emsp;&emsp;这就是<code>Promise</code>的用法。使用<code>Promise</code>来封装异步请求，结构清晰，写法简明，即使有多层嵌套也不会出现回调地狱的情况。.then()方法有一个链式调用的特点。一个<code>Promise</code>对象会返回一个新的<code>Promise</code>对象，所以如果都是<code>Promise</code>对象的话，可以这样用（前提是都用<code>Promise</code>包装起来）。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 以下Promise对象省略了reject函数</span></span><br><span class="line"><span class="keyword">const</span> json = &#123;</span><br><span class="line">  data: &#123;</span><br><span class="line">    list: [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">p1</span>(<span class="params">json</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve</span>) =&gt;</span> &#123;</span><br><span class="line">    setTimeout(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">      <span class="built_in">console</span>.log(json.data)</span><br><span class="line">      <span class="keyword">return</span> resolve(json.data)</span><br><span class="line">    &#125;, <span class="number">1000</span>)</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">p2</span>(<span class="params">data</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve</span>) =&gt;</span> &#123;</span><br><span class="line">    setTimeout(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">      <span class="built_in">console</span>.log(data.list)</span><br><span class="line">      resolve(data.list)</span><br><span class="line">    &#125;, <span class="number">1000</span>)</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">p3</span>(<span class="params">list</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve</span>) =&gt;</span> &#123;</span><br><span class="line">    setTimeout(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">      <span class="built_in">console</span>.log(list[<span class="number">2</span>])</span><br><span class="line">      resolve(list[<span class="number">2</span>])</span><br><span class="line">    &#125;, <span class="number">1000</span>)</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">p1(json).then(p2).then(p3)</span><br><span class="line"></span><br><span class="line"><span class="comment">/* </span></span><br><span class="line"><span class="comment">&#123;list: [1, 2, 3]&#125;</span></span><br><span class="line"><span class="comment">[1, 2, 3]</span></span><br><span class="line"><span class="comment">3</span></span><br><span class="line"><span class="comment">（每行输出间隔1s）</span></span><br><span class="line"><span class="comment">*/</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;上面的代码首先模拟了一个后台请求到的数据，一个json对象，里面包含了data对象，data对象下是一个数组。接下来分别封装了p1, p2, p3三个Promise对象函数。每一层内用setTimeout定时器模拟了执行异步请求，间隔1秒。在定时器里把当前接收到的数据的下一层打印出来，同时以<code>resolve</code>函数参数的形式传递出去。最后执行.then()方法的时候，我们可以看到，每一层函数都打印出了上一层传递进来的数据。也就是说，上层<code>Promise</code>对象传递出来的属性，.then()方法可以接收到。</p>
<h2 id="Promise-catch"><a href="#Promise-catch" class="headerlink" title="Promise.catch()"></a>Promise.catch()</h2><p>&emsp;&emsp;.catch()方法可以捕获到<code>Promise</code>对象内的错误。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">p</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="literal">false</span>) &#123;  <span class="comment">// 如果异步操作成功</span></span><br><span class="line">      resolve()</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;  <span class="comment">// 异步操作失败</span></span><br><span class="line">      reject()</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">p()</span><br><span class="line">  .then(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'success!'</span>)</span><br><span class="line">  &#125;)</span><br><span class="line">  .catch(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'fail!'</span>)</span><br><span class="line">  &#125;)</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;上面的代码用.catch()捕获了<code>Promise</code>对象内模拟的异步请求失败的场景，执行了reject()函数。我们也可以指定.then()方法的第二个参数，也就是执行状态失败时的回调函数来匹配错误的情况，但是实际使用中不推荐这么写，推荐使用.catch()方法来捕获错误状态，同时指定需要执行的函数。</p>
<h2 id="Promise-finally"><a href="#Promise-finally" class="headerlink" title="Promise.finally()"></a>Promise.finally()</h2><p>&emsp;&emsp;.finally()方法接收一个回调函数作为参数，该方法指定<code>Promise</code>对象的状态结束时执行的回调函数。也就是说，不管此时状态为成功还是失败，只要从Pending状态变为了其它两种状态的任意一种，都会执行.finally()方法里的回调函数。<strong>finally()方法的回调函数不接受任何参数。</strong></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">p</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="literal">false</span>) &#123;  <span class="comment">// 如果异步操作成功</span></span><br><span class="line">      resolve()</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;  <span class="comment">// 异步操作失败</span></span><br><span class="line">      reject()</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">p()</span><br><span class="line">  .then(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'success!'</span>)</span><br><span class="line">  &#125;)</span><br><span class="line">  .catch(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'fail!'</span>)</span><br><span class="line">  &#125;)</span><br><span class="line">  .finally(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'finally!'</span>)</span><br><span class="line">  &#125;)</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;上面的代码使用了.finally()方法，无论<code>Promise</code>对象执行的是<code>resolve</code>还是<code>reject</code>，.finally()里的回调函数都会执行！</p>
<h2 id="Promise-all"><a href="#Promise-all" class="headerlink" title="Promise.all()"></a>Promise.all()</h2><p>&emsp;&emsp;在实际使用中，有这样一种情况。我们想监听多个<code>Promise</code>对象的返回情况，如果所有的<code>Promise</code>对象都返回成功状态，才执行成功的回调。.all()方法就是用来干这个的，它接收一个数组参数，数组里的每一项都是一个<code>Promise</code>对象，都返回成功时.all()方法执行成功回调，如果有一项返回失败，则执行失败回调。这个方法的回调参数接收数组内每个<code>Promise</code>对象返回的参数，并按照顺序将它们放到一个数组里。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> p1 = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">  resolve(<span class="number">4</span>)</span><br><span class="line">&#125;)</span><br><span class="line"><span class="keyword">const</span> p2 = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">  resolve(<span class="number">5</span>)</span><br><span class="line">&#125;)</span><br><span class="line"><span class="keyword">const</span> p3 = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">  resolve(<span class="number">6</span>)</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="built_in">Promise</span>.all([p1, p2, p3]).then(<span class="function">(<span class="params">values</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'success!'</span>)</span><br><span class="line">  <span class="built_in">console</span>.log(values)  <span class="comment">// [4, 5, 6]</span></span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;上面的代码先创建里3个<code>Promise</code>对象（省略了模拟异步请求的步骤，直接执行了<code>resolve</code>方法），然后将这三个<code>Promise</code>对象以数组的形式传递给.all()方法，同时每个<code>Promise</code>对象里都分别传递了一个数字参数进去。.all()方法监听到三个<code>Promise</code>对象状态均为成功，则执行.then()方法内的代码，打印出了success!。同时三个<code>Promise</code>对象传递进来的数字参数也被接收到了。</p>
<p>&emsp;&emsp;如果.all()方法的参数里有状态为失败的对象，则.all()方法则执行失败回调，同时接收到的参数为第一个执行<code>reject</code>函数时传递进来的参数。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> p1 = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">  resolve(<span class="number">4</span>)</span><br><span class="line">&#125;)</span><br><span class="line"><span class="keyword">const</span> p2 = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">  reject(<span class="number">5</span>)</span><br><span class="line">&#125;)</span><br><span class="line"><span class="keyword">const</span> p3 = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">  reject(<span class="number">6</span>)</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="built_in">Promise</span>.all([p1, p2, p3])</span><br><span class="line">  .then(<span class="function">(<span class="params">value</span>) =&gt;</span> &#123;  <span class="comment">// 这里不会被执行</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'success!'</span>)</span><br><span class="line">    <span class="built_in">console</span>.log(value)</span><br><span class="line">  &#125;)</span><br><span class="line">  .catch(<span class="function">(<span class="params">value</span>) =&gt;</span> &#123;  <span class="comment">// 执行这里</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'fail!'</span>)</span><br><span class="line">    <span class="built_in">console</span>.log(value)  <span class="comment">// 5</span></span><br><span class="line">  &#125;)</span><br></pre></td></tr></table></figure>
<h2 id="Promise-race"><a href="#Promise-race" class="headerlink" title="Promise.race()"></a>Promise.race()</h2><p>&emsp;&emsp;race译为赛跑，速度竞赛，顾名思义，这个方法就是返回最先改变状态的<code>Promise</code>实例。直接看代码：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> p1 = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">  setTimeout(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    resolve(<span class="number">4</span>)</span><br><span class="line">  &#125;, <span class="number">1000</span>)</span><br><span class="line">&#125;)</span><br><span class="line"><span class="keyword">const</span> p2 = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">  setTimeout(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    resolve(<span class="number">5</span>)</span><br><span class="line">  &#125;, <span class="number">800</span>)</span><br><span class="line">&#125;)</span><br><span class="line"><span class="keyword">const</span> p3 = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">  setTimeout(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    resolve(<span class="number">6</span>)</span><br><span class="line">  &#125;, <span class="number">900</span>)</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="built_in">Promise</span>.race([p1, p2, p3])</span><br><span class="line">  .then(<span class="function">(<span class="params">value</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(value)  <span class="comment">// 5</span></span><br><span class="line">  &#125;)</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;上面代码还是首先封装了三个<code>Promise</code>对象，里面用setTimeout定时器模拟了异步请求，分别为1000ms，800ms和900ms，并分别传递了一个数字参数进去。可以看到最终是p2先执行完，所以打印出的是800ms的p2传递进去的参数5。</p>
]]></content>
      <categories>
        <category>技术</category>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>javascript</tag>
        <tag>ES6</tag>
        <tag>Promise</tag>
      </tags>
  </entry>
  <entry>
    <title>Mac OS下使用自带终端ssh连接服务器</title>
    <url>/2019/mac_ssh.html</url>
    <content><![CDATA[<p>&emsp;&emsp;之前在Windows环境下都是用PuTTY这款软件来连接和保存远程服务器地址的，而在Mac OS下可以使用自带的终端(terminal)直接连接和保存，非常方便。</p>
<h2 id="1-直接连接"><a href="#1-直接连接" class="headerlink" title="1.直接连接"></a>1.直接连接</h2><p>&emsp;&emsp;首先打开Mac下的终端，输入：<code>ssh -p 端口号 服务器用户名@ip</code>，例如：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ssh -p 22 root@192.168.1.8</span><br></pre></td></tr></table></figure>
<a id="more"></a>
<p>&emsp;&emsp;然后终端会让你输入<code>yes</code>或<code>no</code>来询问你是否连接，输入<code>yes</code>后，再输入服务器该账户下的密码，回车，最后连接成功。</p>
<h2 id="2-保存服务器别名连接"><a href="#2-保存服务器别名连接" class="headerlink" title="2.保存服务器别名连接"></a>2.保存服务器别名连接</h2><p>&emsp;&emsp;第一种方法可以直接连接，但是服务器一多，没人记得住所有的连接信息，所以就需要提前把连接信息保存起来并取一个别名，方便以后登录。</p>
<p>&emsp;&emsp;第一步还是先打开终端，使用vim命令编辑<code>~/.ssh</code>路径下的<code>config</code>文件。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#服务器1</span><br><span class="line">Host 服务器别名</span><br><span class="line">	HostName ip地址</span><br><span class="line">	Port 22</span><br><span class="line">	User 用户名</span><br><span class="line">#服务器2</span><br><span class="line">Host 服务器别名</span><br><span class="line">	HostName ip地址</span><br><span class="line">	Port 22</span><br><span class="line">	User 用户名</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;添加好连接信息后，保存，终端下输入<code>ssh 服务器别名</code>，输入密码连接即可。</p>
]]></content>
      <categories>
        <category>技术</category>
        <category>工具</category>
      </categories>
      <tags>
        <tag>Mac OS</tag>
        <tag>ssh</tag>
      </tags>
  </entry>
  <entry>
    <title>Node.js中的events模块</title>
    <url>/2020/node-events.html</url>
    <content><![CDATA[<p>&emsp;&emsp;在Node.js（以下简称node）中，有一个events模块，中文名称是“事件触发器”。node的一大特点就是事件驱动。在node里，很多行为都会触发相应的事件，而所有能触发事件的对象都是events模块类的实例，它们都有添加事件的能力。</p>
<a id="more"></a>
<h2 id="on"><a href="#on" class="headerlink" title="on()"></a>on()</h2><p>&emsp;&emsp;先看代码：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> events = <span class="built_in">require</span>(<span class="string">'events'</span>)</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyEvent</span> <span class="keyword">extends</span> <span class="title">events</span> </span>&#123; &#125;</span><br><span class="line"><span class="keyword">const</span> myevent = <span class="keyword">new</span> MyEvent()</span><br><span class="line"><span class="comment">// 注册监听器</span></span><br><span class="line">myevent.on(<span class="string">'event1'</span>, <span class="function"><span class="keyword">function</span> (<span class="params">a</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'event'</span> + a)</span><br><span class="line">&#125;)</span><br><span class="line"><span class="comment">// 使用ES6箭头函数注册监听器</span></span><br><span class="line">myevent.on(<span class="string">'event2'</span>, (a) =&gt; &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'event'</span> + a)</span><br><span class="line">&#125;)</span><br><span class="line"><span class="comment">// emit()触发事件</span></span><br><span class="line">myevent.emit(<span class="string">'event1'</span>, <span class="number">1</span>)</span><br><span class="line">myevent.emit(<span class="string">'event2'</span>, <span class="number">2</span>)</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;上面的代码使用了一个<code>MyEvent</code>类继承了<code>events</code>类，使<code>MyEvent</code>类的实例有了注册事件监听器的能力。然后调用了<code>on()</code>函数，注册了两个监听器。<code>on()</code>函数可以接收两个参数，第一个参数是事件名称，第二个参数是一个函数，为触发事件时执行的方法，函数的参数为触发事件时要传给监听器的参数，可以没有或有若干个。我注册了两个监听器，其中第二个监听器的第二个函数参数是采用ES6的箭头的写法。两者的区别是箭头函数没有<code>this</code>，而普通函数的<code>this</code>关键字指向监听器所绑定类的实例，在上面的代码里也就是<code>myevent</code>。</p>
<p>&emsp;&emsp;添加好监听器后，用<code>emit()</code>触发事件，后面的参数就是添加监听器时，函数内传递的参数。</p>
<p>&emsp;&emsp;在node环境下运行上面的代码，就会输出：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">event1</span><br><span class="line">event2</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;上面的方法其实继承的是<code>events</code>模块的<code>EventEmitter</code>类，所以我们直接用如下方法也可以。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> EventEmitter = <span class="built_in">require</span>(<span class="string">'events'</span>).EventEmitter</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> myevent = <span class="keyword">new</span> EventEmitter()</span><br><span class="line">myevent.on(<span class="string">'event'</span>, () =&gt; &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'event'</span>)  <span class="comment">// event</span></span><br><span class="line">&#125;)</span><br><span class="line">myevent.emit(<span class="string">'event'</span>)</span><br></pre></td></tr></table></figure>
<h2 id="once"><a href="#once" class="headerlink" title="once()"></a>once()</h2><p>&emsp;&emsp;除了用<code>on()</code>函数添加监听器，还可以用<code>once()</code>添加仅可以被触发一次的事件。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> events = <span class="built_in">require</span>(<span class="string">'events'</span>)</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyEvent</span> <span class="keyword">extends</span> <span class="title">events</span> </span>&#123; &#125;</span><br><span class="line"><span class="keyword">const</span> myevent = <span class="keyword">new</span> MyEvent()</span><br><span class="line">myevent.once(<span class="string">'event'</span>, () =&gt; &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'event'</span>)</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line">myevent.emit(<span class="string">'event'</span>)</span><br><span class="line"><span class="comment">// 用once()添加的触发器只会被触发一次，下面的语句不会被执行。</span></span><br><span class="line">myevent.emit(<span class="string">'event'</span>)</span><br></pre></td></tr></table></figure>
<h2 id="setMaxListeners-n"><a href="#setMaxListeners-n" class="headerlink" title="setMaxListeners(n)"></a>setMaxListeners(n)</h2><p>&emsp;&emsp;默认情况下，如果为特定事件添加了超过 <code>10</code> 个监听器，则 <code>EventEmitter</code> 会打印一个警告。 这有助于发现内存泄露。使用<code>setMaxlisteners()</code>方法，可以调整这个限制。参数为一个数字，即次数，参数为0表示不限次数。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> events = <span class="built_in">require</span>(<span class="string">'events'</span>)</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyEvent</span> <span class="keyword">extends</span> <span class="title">events</span> </span>&#123; &#125;</span><br><span class="line"><span class="keyword">const</span> myevent = <span class="keyword">new</span> MyEvent()</span><br><span class="line">myevent.setMaxListeners(<span class="number">11</span>)  <span class="comment">// 修改事件监听器的数量为11</span></span><br><span class="line"><span class="comment">// 给event事件添加11个监听器</span></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt;= <span class="number">10</span>; i++) &#123;</span><br><span class="line">  myevent.on(<span class="string">'event'</span>, () =&gt; &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">`event<span class="subst">$&#123;i + <span class="number">1</span>&#125;</span>`</span>)</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">myevent.emit(<span class="string">'event'</span>)</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;上面的代码使用<code>setMaxListeners()</code>方法设置了<code>myevent</code>可以添加11个监听器（超过控制台会打印警告）。</p>
<h2 id="removeListener"><a href="#removeListener" class="headerlink" title="removeListener()"></a>removeListener()</h2><p>&emsp;&emsp;如果需要删除某个事件监听器的回调的话，可以使用<code>removeListener()</code>方法来操作。该方法接收两个参数，第一个参数为事件名称，第二个参数为事件的回调名称。（这里说的回调其实就是当前监听事件的执行函数）</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> events = <span class="built_in">require</span>(<span class="string">'events'</span>)</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyEvent</span> <span class="keyword">extends</span> <span class="title">events</span> </span>&#123; &#125;</span><br><span class="line"><span class="keyword">const</span> myevent = <span class="keyword">new</span> MyEvent()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">callback1</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'callback1'</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">callback2</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'callback2'</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">myevent.on(<span class="string">'event'</span>, callback1)</span><br><span class="line">myevent.on(<span class="string">'event'</span>, callback2)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 删除callback1回调</span></span><br><span class="line">myevent.removeListener(<span class="string">'event'</span>, callback1)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 触发事件</span></span><br><span class="line">myevent.emit(<span class="string">'event'</span>)</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;执行上面的代码后，只有callback2被触发执行了。</p>
<h2 id="removeAllListeners"><a href="#removeAllListeners" class="headerlink" title="removeAllListeners()"></a>removeAllListeners()</h2><p>&emsp;&emsp;如果我们要删除所有的监听事件，或者删除某个监听事件下所有的执行函数，可以用<code>removeAllListeners()</code>方法。这个方法接收一个可选参数，为事件名称。传入事件名称将删除该事件名称下的所有执行函数。不传入参数将删除所有的监听事件。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> events = <span class="built_in">require</span>(<span class="string">'events'</span>)</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyEvent</span> <span class="keyword">extends</span> <span class="title">events</span> </span>&#123; &#125;</span><br><span class="line"><span class="keyword">const</span> myevent = <span class="keyword">new</span> MyEvent()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">callback1</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'callback1'</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">callback2</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'callback2'</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 这里添加了两个监听器</span></span><br><span class="line">myevent.on(<span class="string">'event1'</span>, callback1)</span><br><span class="line">myevent.on(<span class="string">'event2'</span>, callback2)</span><br><span class="line"></span><br><span class="line">myevent.removeAllListeners(<span class="string">'event1'</span>)  <span class="comment">// 删除event1监听事件</span></span><br><span class="line"><span class="comment">// myevent.removeAllListeners()  // 如果执行这一句，event1和event2都将被删除</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 触发事件</span></span><br><span class="line">myevent.emit(<span class="string">'event1'</span>)</span><br><span class="line">myevent.emit(<span class="string">'event2'</span>)</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>技术</category>
        <category>Node.js</category>
      </categories>
      <tags>
        <tag>javascript</tag>
        <tag>Node.js</tag>
        <tag>node</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript设计模式（更新中…）</title>
    <url>/2021/js-design-patterns.html</url>
    <content><![CDATA[<h1 id="JavaScript的23种设计模式"><a href="#JavaScript的23种设计模式" class="headerlink" title="JavaScript的23种设计模式"></a>JavaScript的23种设计模式</h1><table>
<thead>
<tr>
<th>范围/目的</th>
<th>创建型模式</th>
<th>结构型模式</th>
<th>行为型模式</th>
</tr>
</thead>
<tbody>
<tr>
<td>类模式</td>
<td>工厂方法</td>
<td>（类）适配器</td>
<td>模板方法 解释器</td>
</tr>
<tr>
<td>对象模式</td>
<td>单例 原型 抽象工厂 建造者</td>
<td>代理 （对象）适配器 桥接 装饰 外观 享元 组合</td>
<td>策略 命令 职责链 状态 观察者 中介者 迭代器 访问者 备忘录</td>
</tr>
</tbody>
</table>
<h1 id="什么是设计模式"><a href="#什么是设计模式" class="headerlink" title="什么是设计模式"></a>什么是设计模式</h1><blockquote>
<p>在软件工程中，<strong>设计模式（design pattern）是对软件设计中普遍存在（反复出现）的各种问题，所提出的解决方案。</strong>这个术语是由埃里希·伽玛（Erich Gamma）等人在1990年代从建筑设计领域引入到计算机科学的。</p>
</blockquote>
<blockquote>
<p><strong>设计模式并不直接用来完成代码的编写，而是描述在各种不同情况下，要怎么解决问题的一种方案。</strong>面向对象设计模式通常以类别或对象来描述其中的关系和相互作用，但不涉及用来完成应用程序的特定类别或对象。设计模式能使不稳定依赖于相对稳定、具体依赖于相对抽象，避免会引起麻烦的紧耦合，以增强软件设计面对并适应变化的能力。</p>
</blockquote>
<blockquote>
<p>并非所有的软件模式都是设计模式，设计模式特指软件“设计”层次上的问题。还有其他非设计模式的模式，如架构模式。同时，算法不能算是一种设计模式，因为算法主要是用来解决计算上的问题，而非设计上的问题。</p>
</blockquote>
<h1 id="设计模式"><a href="#设计模式" class="headerlink" title="设计模式"></a>设计模式</h1><h2 id="单例（Singleton）模式"><a href="#单例（Singleton）模式" class="headerlink" title="单例（Singleton）模式"></a>单例（Singleton）模式</h2><p>单例模式即<strong>保证一个类仅能有一个实例，且全局可以访问该实例</strong>。</p>
<p>其实现原理是：创建实例时，缓存实例，后面创建时先判断有没有创建过，有的话直接返回。也就是说主要实现逻辑在缓存实例这里，我们可以用静态属性、闭包、重写构造函数等几种不同的方式来实现单例模式。</p>
<p>单例模式实际大多应用在只存在唯一一个实例的场景，如登录框、统一页面浮窗等。</p>
<h3 id="静态属性实现单例模式"><a href="#静态属性实现单例模式" class="headerlink" title="静态属性实现单例模式"></a>静态属性实现单例模式</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// ES5版本</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Singleton</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (Singleton.instance) &#123;</span><br><span class="line">    <span class="keyword">return</span> Singleton.instance <span class="comment">// 如果有缓存直接返回</span></span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">this</span>.name = name</span><br><span class="line">  <span class="keyword">return</span> Singleton.instance = <span class="keyword">this</span> <span class="comment">// 将静态属性instance设置为首个实例（缓存）</span></span><br><span class="line">&#125;</span><br><span class="line">Singleton.prototype.sayName = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="keyword">this</span>.name)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> s1 = <span class="keyword">new</span> Singleton(<span class="string">'s1'</span>)</span><br><span class="line"><span class="keyword">const</span> s2 = <span class="keyword">new</span> Singleton(<span class="string">'s2'</span>)</span><br><span class="line"><span class="built_in">console</span>.log(s1 === s2) <span class="comment">// true，说明两个实例是同一个</span></span><br><span class="line">s2.sayName() <span class="comment">// 's1'</span></span><br></pre></td></tr></table></figure>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// ES6版本</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> </span>&#123;</span><br><span class="line">  <span class="keyword">static</span> instance <span class="comment">// 定义一个静态属性</span></span><br><span class="line">  <span class="keyword">constructor</span>(name) &#123;</span><br><span class="line">    <span class="keyword">if</span> (Singleton.instance) &#123;</span><br><span class="line">      <span class="keyword">return</span> Singleton.instance <span class="comment">// 如果有缓存直接返回</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">this</span>.name = name</span><br><span class="line">    <span class="keyword">return</span> Singleton.instance = <span class="keyword">this</span> <span class="comment">// 将静态属性instance设置为首个实例（缓存）</span></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  sayName() &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span>.name)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> s1 = <span class="keyword">new</span> Singleton(<span class="string">'s1'</span>)</span><br><span class="line"><span class="keyword">const</span> s2 = <span class="keyword">new</span> Singleton(<span class="string">'s2'</span>)</span><br><span class="line"><span class="built_in">console</span>.log(s1 === s2) <span class="comment">// true，说明两个实例是同一个</span></span><br><span class="line">s2.sayName() <span class="comment">// 's1'</span></span><br></pre></td></tr></table></figure>
<h3 id="闭包实现单例模式"><a href="#闭包实现单例模式" class="headerlink" title="闭包实现单例模式"></a>闭包实现单例模式</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> Singleton = (<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> instance = <span class="literal">null</span> <span class="comment">// 设置私有变量instance</span></span><br><span class="line">  <span class="keyword">const</span> CreateSingleton = <span class="function"><span class="keyword">function</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (instance) &#123;</span><br><span class="line">      <span class="keyword">return</span> instance</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">this</span>.name = name</span><br><span class="line">    <span class="keyword">return</span> instance = <span class="keyword">this</span></span><br><span class="line">  &#125;</span><br><span class="line">  CreateSingleton.prototype.sayName = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span>.name)</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> CreateSingleton</span><br><span class="line">&#125;())</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> s1 = <span class="keyword">new</span> Singleton(<span class="string">'s1'</span>)</span><br><span class="line"><span class="keyword">const</span> s2 = <span class="keyword">new</span> Singleton(<span class="string">'s2'</span>)</span><br><span class="line"><span class="built_in">console</span>.log(s1 === s2) <span class="comment">// true</span></span><br><span class="line">s2.sayName() <span class="comment">// 's1'</span></span><br></pre></td></tr></table></figure>
<h3 id="重写构造函数实现单例模式"><a href="#重写构造函数实现单例模式" class="headerlink" title="重写构造函数实现单例模式"></a>重写构造函数实现单例模式</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Singleton</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// 缓存实例</span></span><br><span class="line">  <span class="keyword">let</span> instance</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 重写构造函数</span></span><br><span class="line">  Singleton = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.name = name</span><br><span class="line">    <span class="keyword">return</span> instance</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 保留原型</span></span><br><span class="line">  Singleton.prototype = <span class="keyword">this</span>.constructor.prototype <span class="comment">// 将重写后的构造函数的原型指回原构造函数的原型，使首次声明的实例可以正确访问到原型链上的属性与方法（Singleton.prototype.a = 1，使其生效）</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// 定义原型方法</span></span><br><span class="line">  Singleton.prototype.sayName = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span>.name)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 创建实例</span></span><br><span class="line">  instance = <span class="keyword">new</span> Singleton()</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 设置构造函数</span></span><br><span class="line">  instance.constructor = Singleton <span class="comment">// 将实例的构造函数指向被重写后的Singleton</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> instance <span class="comment">// 首次使用该构造函数时，直接返回instance实例。再次使用时因为构造函数已被重写，一样返回instance实例。</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">Singleton.prototype.a = <span class="number">1</span> <span class="comment">// 此时的代码是给重写前的构造函数的原型上添加了属性a</span></span><br><span class="line"><span class="keyword">const</span> s1 = <span class="keyword">new</span> Singleton(<span class="string">'s1'</span>)</span><br><span class="line"><span class="keyword">const</span> s2 = <span class="keyword">new</span> Singleton(<span class="string">'s2'</span>)</span><br><span class="line"><span class="built_in">console</span>.log(s1.constructor) <span class="comment">// 此时s1.constructor指向重写后的构造函数</span></span><br><span class="line"><span class="built_in">console</span>.log(s1 === s2) <span class="comment">// true</span></span><br><span class="line">s2.sayName() <span class="comment">// 's1'</span></span><br><span class="line"><span class="built_in">console</span>.log(s1.a) <span class="comment">// 1</span></span><br></pre></td></tr></table></figure>
<h3 id="通用（惰性）单例模式"><a href="#通用（惰性）单例模式" class="headerlink" title="通用（惰性）单例模式"></a>通用（惰性）单例模式</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> createSingleton = <span class="function"><span class="keyword">function</span>(<span class="params">fn</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> result = <span class="literal">null</span></span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> result || (result = fn.apply(<span class="keyword">this</span>, <span class="built_in">arguments</span>))</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>以上方法用于创建通用（惰性）单例模式。该方法创建出来的单例模式还具有惰性的特点，真正调用的时候单例才会被创建，而不是页面加载后立即被创建。</p>
<p>用法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">test</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> &#123; <span class="attr">a</span>: <span class="number">1</span> &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> s = createSingleton(test) <span class="comment">// 使用通用单例模式封装</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> t1 = s()</span><br><span class="line"><span class="keyword">const</span> t2 = s()</span><br><span class="line"><span class="built_in">console</span>.log(t1 === t2) <span class="comment">// true，此时生成的两个对象全等，其实返回的始终是一个对象</span></span><br></pre></td></tr></table></figure>
<h2 id="原型（Prototype）模式"><a href="#原型（Prototype）模式" class="headerlink" title="原型（Prototype）模式"></a>原型（Prototype）模式</h2><p><strong>原型模式（prototype）是指用原型实例指向创建对象的种类，并且通过拷贝这些原型创建新的对象。</strong> 原型模式不单是一种设计模式，也被称为一种编程泛型。 从设计模式的角度讲，原型模式是用于创建对象的一种模式。我们不再关心对象的具体类型，而是找到一个对象，然后通过克隆来创建一个一模一样的对象<strong>（使用原型模式，并不是为了得到一个副本，而是为了得到与构造函数（类）相对应的类型的实例、实现数据/方法的共享）</strong>。在其他语言很少使用原型模式，但是JavaScript作为原型语言，在构造新对象及其原型时会使用该模式。</p>
<p>原型上的属性与方法是被实例共享的：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Person</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>.name = name</span><br><span class="line">  <span class="keyword">this</span>.sayHi = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123; <span class="comment">// 实例方法</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'Hi'</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 在原型上添加方法</span></span><br><span class="line">Person.prototype.sayName = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="keyword">this</span>.name)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> p1 = <span class="keyword">new</span> Person(<span class="string">'张三'</span>)</span><br><span class="line"><span class="keyword">const</span> p2 = <span class="keyword">new</span> Person(<span class="string">'李四'</span>)</span><br><span class="line"><span class="built_in">console</span>.log(p1.sayHi === p2.sayHi) <span class="comment">// false</span></span><br><span class="line"><span class="built_in">console</span>.log(p1.sayName === p2.sayName) <span class="comment">// true</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 因为使用原型模式可以使示例共享属性和方法，避免重复创建，可以带来性能上的提升</span></span><br></pre></td></tr></table></figure>
<p>利用以上特性，我们可以实现使用原型模式创建新对象。</p>
<p>ECMAScript 5新增了 <a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Object/create" target="_blank" rel="noopener">Object.create()</a> 方法可以直接使用原型模式创建新对象。</p>
<blockquote>
<p><code>Object.create()</code> 方法创建一个新对象，使用现有的对象来提供新创建的对象的<code>__proto__</code>。</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 以下示例仅展示Object.create()方法第一个参数的用法</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> obj = &#123;</span><br><span class="line">  a: <span class="number">1</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">const</span> obj2 = <span class="built_in">Object</span>.create(obj)</span><br><span class="line"><span class="built_in">console</span>.log(obj2.a) <span class="comment">// 1</span></span><br><span class="line"><span class="built_in">console</span>.log(obj2.__proto__ === obj) <span class="comment">// true</span></span><br></pre></td></tr></table></figure>
<p>但是需要注意从原型上继承来的属性和方法具有<strong>动态性</strong>：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">obj.a = <span class="number">2</span></span><br><span class="line"><span class="built_in">console</span>.log(obj2.a) <span class="comment">// 2</span></span><br></pre></td></tr></table></figure>
<p>Object.create()方法的实现过程（第一个参数）:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 核心思想是创建一个新的函数，将新函数的prototype属性指向原函数，再返回这个新创建的函数</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">object</span>(<span class="params">o</span>) </span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">F</span>(<span class="params"></span>) </span>&#123;&#125;</span><br><span class="line">  F.prototype = o</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> F()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="工厂方法（Factory-Method）模式"><a href="#工厂方法（Factory-Method）模式" class="headerlink" title="工厂方法（Factory Method）模式"></a>工厂方法（Factory Method）模式</h2><p>工厂方法模式是<strong>一个用于创建对象的接口，它提供了一种将实例化逻辑委托给子类的方法。工厂方法允许一个类延迟实例化它使用的子类</strong>。</p>
<p>先来看一下<strong>工厂模式</strong>。</p>
<h3 id="工厂模式"><a href="#工厂模式" class="headerlink" title="工厂模式"></a>工厂模式</h3><p>工厂模式即抽象了创建 具体对象的过程，将创建对象的过程进行了单独的封装。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 一个简单的工厂模式的示例</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">createPerson</span>(<span class="params">name, age</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> o = <span class="keyword">new</span> <span class="built_in">Object</span>()</span><br><span class="line">  o.name = name</span><br><span class="line">  o.age = age</span><br><span class="line">  o.sayName = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span>.name)</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> o</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> p1 = createPerson(<span class="string">'张三'</span>, <span class="number">18</span>)</span><br><span class="line"><span class="keyword">const</span> p2 = createPerson(<span class="string">'李四'</span>, <span class="number">20</span>)</span><br><span class="line">p1.sayName() <span class="comment">// '张三'</span></span><br></pre></td></tr></table></figure>
<h3 id="工厂方法模式"><a href="#工厂方法模式" class="headerlink" title="工厂方法模式"></a>工厂方法模式</h3><p>正如工厂方法模式的定义所说：</p>
<blockquote>
<p>一个用于创建对象的接口，它提供了一种将实例化逻辑委托给子类的方法。工厂方法允许一个类延迟实例化它使用的子类。</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 一个使用工厂方法模式创建员工的示例</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Person</span>(<span class="params">name, age</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>.name = name</span><br><span class="line">  <span class="keyword">this</span>.age = age</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">CreateProgrammer</span>(<span class="params">name, age</span>) </span>&#123;</span><br><span class="line">  Person.apply(<span class="keyword">this</span>, <span class="built_in">arguments</span>) <span class="comment">// 继承基类</span></span><br><span class="line">  <span class="keyword">this</span>.work = <span class="string">'write the code'</span></span><br><span class="line">  <span class="keyword">this</span>.tools = [<span class="string">'computer'</span>, <span class="string">'keyboard'</span>]</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">CreateDriver</span>(<span class="params">name, age</span>) </span>&#123;</span><br><span class="line">  Person.apply(<span class="keyword">this</span>, <span class="built_in">arguments</span>) <span class="comment">// 继承基类</span></span><br><span class="line">  <span class="keyword">this</span>.work = <span class="string">'drive'</span></span><br><span class="line">  <span class="keyword">this</span>.tools = [<span class="string">'car'</span>]</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">createStaff</span>(<span class="params">name, age, job</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">switch</span>(job) &#123; <span class="comment">// 根据参数实例化相应的类</span></span><br><span class="line">    <span class="keyword">case</span> <span class="string">'Programmer'</span>:</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">new</span> CreateProgrammer(name, age)</span><br><span class="line">    <span class="keyword">case</span> <span class="string">'Driver'</span>:</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">new</span> CreateDriver(name, age)</span><br><span class="line">    <span class="keyword">default</span>:</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">'参数错误，可选参数: [Programmer, Driver]'</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> p1 = createStaff(<span class="string">'张三'</span>, <span class="number">18</span>, <span class="string">'Programmer'</span>)</span><br><span class="line"><span class="keyword">const</span> p2 = createStaff(<span class="string">'李四'</span>, <span class="number">19</span>, <span class="string">'Driver'</span>)</span><br></pre></td></tr></table></figure>
<p>上面的示例可以让我们通过传入员工的基本信息来创建出任意多个员工，不过员工的类型无法扩展，下面来看一个优化后的示例。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Person</span>(<span class="params">name, age</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>.name = name</span><br><span class="line">  <span class="keyword">this</span>.age = age</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">createStaff</span>(<span class="params">name, age, job</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="keyword">typeof</span> createStaff.prototype[job] !== <span class="string">'function'</span>) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">'员工类型错误！'</span>)</span><br><span class="line">  &#125;</span><br><span class="line">	<span class="comment">// createStaff是一个普通函数，而非构造函数，首次不加new运算符使用时，会跑入else代码块</span></span><br><span class="line">  <span class="keyword">if</span> (<span class="keyword">this</span> <span class="keyword">instanceof</span> createStaff) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="keyword">this</span>[job](name, age)</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> createStaff(...arguments)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">createStaff.prototype = &#123;</span><br><span class="line">  <span class="string">'Programmer'</span>: <span class="function"><span class="keyword">function</span>(<span class="params">name, age</span>) </span>&#123;</span><br><span class="line">    Person.call(<span class="keyword">this</span>, name, age) <span class="comment">// 继承基类</span></span><br><span class="line">    <span class="keyword">this</span>.work = <span class="string">'write the code'</span></span><br><span class="line">    <span class="keyword">this</span>.tools = [<span class="string">'computer'</span>, <span class="string">'keyboard'</span>]</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 添加一个员工类型</span></span><br><span class="line">createStaff.prototype.Driver = <span class="function"><span class="keyword">function</span>(<span class="params">name, age</span>) </span>&#123;</span><br><span class="line">  Person.call(<span class="keyword">this</span>, name, age) <span class="comment">// 继承基类</span></span><br><span class="line">  <span class="keyword">this</span>.work = <span class="string">'drive'</span></span><br><span class="line">  <span class="keyword">this</span>.tools = [<span class="string">'car'</span>]</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> p1 = createStaff(<span class="string">'张三'</span>, <span class="number">18</span>, <span class="string">'Programmer'</span>)</span><br><span class="line"><span class="keyword">const</span> p2 = createStaff(<span class="string">'李四'</span>, <span class="number">19</span>, <span class="string">'Driver'</span>)</span><br></pre></td></tr></table></figure>
<h2 id="抽象工厂（Abstract-Factory）模式"><a href="#抽象工厂（Abstract-Factory）模式" class="headerlink" title="抽象工厂（Abstract Factory）模式"></a>抽象工厂（Abstract Factory）模式</h2><p><strong>抽象工厂模式</strong>(Abstract Factory)就是通过类的抽象使得业务适用于一个产品类簇的创建，而不负责某一类产品的实例。</p>
<p>抽象工厂模式需要有抽象类来为子类指明必须实现哪些方法，而目前JavaScript没有抽象类的概念，<code>abstract</code> 曾在JavaScript中是保留字（ECMAScript1-3）。我们可以用 <code>throw</code> 语句抛出异常的方式来模拟抽象类和抽象方法。抽象类无法直接被实例化，必须被子类继承使用。而继承了抽象类的子类也必须自行覆盖抽象类中的抽象方法，否则当子类直接调用从抽象类中继承过来的抽象方法时会报错。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 首先声明四个实体类</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Programmer</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>.title = <span class="string">'开发者'</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Driver</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>.title = <span class="string">'司机'</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Monitor</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>.title = <span class="string">'班长'</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">GroupLeader</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>.title = <span class="string">'组长'</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 接着定义抽象工厂方法AbstractFactory</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">AbstractFactory</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">	<span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">'抽象工厂类无法直接调用'</span>)</span><br><span class="line">&#125;</span><br><span class="line">AbstractFactory.prototype.createStaff = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">'无法直接调用抽象方法，子类请实现接口'</span>)</span><br><span class="line">&#125;</span><br><span class="line">AbstractFactory.prototype.createStudent= <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">'无法直接调用抽象方法，子类请实现接口'</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义创建员工工厂方法</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">StaffFactory</span>(<span class="params"></span>) </span>&#123;&#125;</span><br><span class="line">StaffFactory.prototype = <span class="built_in">Object</span>.create(AbstractFactory.prototype) <span class="comment">// 继承抽象工厂方法</span></span><br><span class="line">StaffFactory.prototype.constructor = StaffFactory <span class="comment">// 保持构造函数具有正确的指向</span></span><br><span class="line">StaffFactory.prototype.createStaff = <span class="function"><span class="keyword">function</span>(<span class="params">job</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">switch</span>(job) &#123;</span><br><span class="line">    <span class="keyword">case</span> <span class="string">'programmer'</span>:</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">new</span> Programmer()</span><br><span class="line">    <span class="keyword">case</span> <span class="string">'driver'</span>:</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">new</span> Driver()</span><br><span class="line">    <span class="keyword">default</span>:</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">'参数错误，可选参数: [programmer, driver]'</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 定义创建学生工厂方法</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">StudentFactory</span>(<span class="params"></span>) </span>&#123;&#125;</span><br><span class="line">StudentFactory.prototype = <span class="built_in">Object</span>.create(AbstractFactory.prototype)</span><br><span class="line">StudentFactory.prototype.constructor = StudentFactory</span><br><span class="line">StudentFactory.prototype.createStudent = <span class="function"><span class="keyword">function</span>(<span class="params">job</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">switch</span>(job) &#123;</span><br><span class="line">    <span class="keyword">case</span> <span class="string">'monitor'</span>:</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">new</span> Monitor()</span><br><span class="line">    <span class="keyword">case</span> <span class="string">'group-leader'</span>:</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">new</span> GroupLeader()</span><br><span class="line">    <span class="keyword">default</span>:</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">'参数错误，可选参数: [monitor, group-leader]'</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Factory</span>(<span class="params">type</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">switch</span>(type) &#123;</span><br><span class="line">    <span class="keyword">case</span> <span class="string">'staff'</span>:</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">new</span> StaffFactory()</span><br><span class="line">    <span class="keyword">case</span> <span class="string">'student'</span>:</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">new</span> StudentFactory()</span><br><span class="line">    <span class="keyword">default</span>:</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">'工厂方法不存在'</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// 用法</span></span><br><span class="line"><span class="keyword">const</span> staff = <span class="keyword">new</span> Factory(<span class="string">'staff'</span>) <span class="comment">// 创建员工工厂方法</span></span><br><span class="line"><span class="keyword">const</span> staffItem = staff.createStaff(<span class="string">'programmer'</span>) <span class="comment">// 根据员工实体类创建的员工</span></span><br><span class="line"><span class="built_in">console</span>.log(staffItem.title) <span class="comment">// '开发者'</span></span><br><span class="line"><span class="keyword">const</span> student = <span class="keyword">new</span> Factory(<span class="string">'student'</span>)</span><br><span class="line"><span class="keyword">const</span> studentItem = student.createStudent(<span class="string">'monitor'</span>)</span><br><span class="line"><span class="built_in">console</span>.log(studentItem.title) <span class="comment">// '班长'</span></span><br></pre></td></tr></table></figure>
<h2 id="建造者（Builder）模式"><a href="#建造者（Builder）模式" class="headerlink" title="建造者（Builder）模式"></a>建造者（Builder）模式</h2><p>建造者模式将一个复杂的对象分解成多个简单的对象来进行构建，分步构建一个复杂对象，并允许按步骤构造。同样的构建过程可以采用不同的表示，将一个复杂对象的 构建层与其表示层分离。</p>
<p>建造者模式包含建造者类、指挥者类和最终生成的产品。用户无需知道用怎样的方式来装配对象，只需要给指挥者类传入类型，由指挥者类决定以怎样的方式装配对象，以得到最终的产品。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 定义一个用户类（建造者类）</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">UserBuilder</span>(<span class="params"></span>) </span>&#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 在用户类上添加方法</span></span><br><span class="line">UserBuilder.prototype.setName = <span class="function"><span class="keyword">function</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>.name = name</span><br><span class="line">&#125;</span><br><span class="line">UserBuilder.prototype.setGender = <span class="function"><span class="keyword">function</span> (<span class="params">gender</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">switch</span>(<span class="built_in">parseInt</span>(gender)) &#123;</span><br><span class="line">    <span class="keyword">case</span> <span class="number">1</span>:</span><br><span class="line">      <span class="keyword">this</span>.gender = <span class="string">'男'</span></span><br><span class="line">      <span class="keyword">break</span></span><br><span class="line">    <span class="keyword">case</span> <span class="number">2</span>:</span><br><span class="line">      <span class="keyword">this</span>.gender = <span class="string">'女'</span></span><br><span class="line">      <span class="keyword">break</span></span><br><span class="line">    <span class="keyword">default</span>:</span><br><span class="line">      <span class="keyword">this</span>.gender = <span class="string">'未知'</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">UserBuilder.prototype.setAge = <span class="function"><span class="keyword">function</span>(<span class="params">age</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>.age = age</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义一个指挥者类</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">UserDirector</span>(<span class="params">name, gender, age</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> user = <span class="keyword">new</span> UserBuilder()</span><br><span class="line">  user.setName(name)</span><br><span class="line">  user.setGender(gender)</span><br><span class="line">  user.setAge(age)</span><br><span class="line">  <span class="keyword">return</span> user</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用（生成一个产品）</span></span><br><span class="line"><span class="keyword">const</span> user1 = UserDirector(<span class="string">'小明'</span>, <span class="number">1</span>, <span class="number">18</span>)</span><br><span class="line"><span class="built_in">console</span>.log(user1) <span class="comment">// &#123;name: "小明", gender: "男", age: 18&#125;</span></span><br></pre></td></tr></table></figure>
<p>使用结合链式调用的创建者模式：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 定义一个用户类（建造者类）</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">UserBuilder</span>(<span class="params"></span>) </span>&#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 在用户类上添加方法</span></span><br><span class="line">UserBuilder.prototype.setName = <span class="function"><span class="keyword">function</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>.name = name</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">this</span></span><br><span class="line">&#125;</span><br><span class="line">UserBuilder.prototype.setGender = <span class="function"><span class="keyword">function</span> (<span class="params">gender</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">switch</span>(<span class="built_in">parseInt</span>(gender)) &#123;</span><br><span class="line">    <span class="keyword">case</span> <span class="number">1</span>:</span><br><span class="line">      <span class="keyword">this</span>.gender = <span class="string">'男'</span></span><br><span class="line">      <span class="keyword">break</span></span><br><span class="line">    <span class="keyword">case</span> <span class="number">2</span>:</span><br><span class="line">      <span class="keyword">this</span>.gender = <span class="string">'女'</span></span><br><span class="line">      <span class="keyword">break</span></span><br><span class="line">    <span class="keyword">default</span>:</span><br><span class="line">      <span class="keyword">this</span>.gender = <span class="string">'未知'</span></span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">this</span></span><br><span class="line">&#125;</span><br><span class="line">UserBuilder.prototype.setAge = <span class="function"><span class="keyword">function</span>(<span class="params">age</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>.age = age</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">this</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用（在这里产品也充当了指挥者的角色，自行选配需要的对象）</span></span><br><span class="line"><span class="keyword">const</span> user1 = <span class="keyword">new</span> UserBuilder()</span><br><span class="line">  .setName(<span class="string">'小明'</span>)</span><br><span class="line">  .setAge(<span class="number">20</span>);</span><br><span class="line"><span class="built_in">console</span>.log(user1) <span class="comment">// &#123;name: "小明", age: 20&#125;</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>技术</category>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>javascript</tag>
        <tag>设计模式</tag>
      </tags>
  </entry>
  <entry>
    <title>Vue.js实例的生命周期</title>
    <url>/2019/vue.js-lifecycle.html</url>
    <content><![CDATA[<p>&emsp;&emsp;vue实例有一个完整的生命周期，先上一段vue.js官网对vue实例生命周期的解释：</p>
<blockquote>
<p>每个 Vue 实例在被创建时都要经过一系列的初始化过程——例如，需要设置数据监听、编译模板、将实例挂载到 DOM 并在数据变化时更新 DOM 等。</p>
</blockquote>
<p>&emsp;&emsp;同时，在这个过程中，会运行一些叫做<strong>生命周期钩子</strong>的函数，这给了用户在不同阶段添加自己的代码的机会。</p>
<a id="more"></a>
<p><img src="https://cdn.xiekaifei.com/image/2019/blog/jishu/lifecycle.png" alt="vue实例生命周期图"></p>
<p>&emsp;&emsp;上面这张图是vuejs官网<a href="https://cn.vuejs.org/v2/guide/instance.html" target="_blank" rel="noopener">vue实例章节</a>中给出的vue实例生命周期图示，下面我来逐项解释图中的每一步当中生命周期钩子函数的用法。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> test = <span class="keyword">new</span> Vue(&#123;</span><br><span class="line">    el: <span class="string">'#app'</span>,</span><br><span class="line">    data: &#123;</span><br><span class="line">        message: <span class="string">'Hello'</span></span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="comment">/*   结合上面的生命周期图来看，首先是第一步创建一个vue对象new Vue(),然后vue实例进行</span></span><br><span class="line"><span class="comment">    基础的初始化操作(Init Events &amp; Lifecycle)之后，就会进入到beforeCreate函数，这时</span></span><br><span class="line"><span class="comment">    控制台会输出“undefined beforeCreate"，这是因为此时vue还没有监控data对象数据的变化，</span></span><br><span class="line"><span class="comment">    所以this.message会输出“undefined”   */</span></span><br><span class="line">    beforeCreate: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="keyword">this</span>.message + <span class="string">' beforeCreate'</span>)</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="comment">/*   beforeCreated函数执行完毕后，vue实例会初始化剩下的部分，接着执行created函数，</span></span><br><span class="line"><span class="comment">    下面的函数会在控制台输出“Hello created”,此时vue实例都已初始化完毕，this.message可以被输出   */</span></span><br><span class="line">    created: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="keyword">this</span>.message + <span class="string">' created'</span>)</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="comment">/*   created函数执行完毕后，会询问实例中是否有"el"选项，本test实例中有el实例，接着会询问是否有</span></span><br><span class="line"><span class="comment">    "template"模板，本实例中没有定义，所以会走No分支，会把当前el的整体内容当做模板。</span></span><br><span class="line"><span class="comment">    那么此时的模板，就是当前HTML文件内，id为"app"标签的所有内容。如果实例中有"template"，</span></span><br><span class="line"><span class="comment">    模板即为"template"属性中的内容。</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">    接着往下走到了beforeMount函数，下面的函数会在控制台输出"Hello beforeMount"，此时</span></span><br><span class="line"><span class="comment">    页面还没有将编译出的HTML文件渲染到页面上，beforeMount函数正是此时执行的函数。   */</span></span><br><span class="line">    beforeMount: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="keyword">this</span>.message + <span class="string">' beforeMount'</span>)</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="comment">// 到了mounted函数执行的时候，此时模板已经挂载完成，HTML内容已经被渲染到了页面上。</span></span><br><span class="line">    mounted: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="keyword">this</span>.message + <span class="string">' mounted'</span>)</span><br><span class="line">    &#125;,</span><br><span class="line">    beforeDestroy: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="keyword">this</span>.message + <span class="string">' beforeDestroy'</span>)</span><br><span class="line">    &#125;,</span><br><span class="line">    destroyed: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="keyword">this</span>.message + <span class="string">' destroyed'</span>)</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="comment">/*   beforeDestroy和destroyed分别是在实例销毁之前和实例销毁之后执行的函数，</span></span><br><span class="line"><span class="comment">    此时如果我们在控制台使用"test.$destroy()"方法来销毁该实例的话，控制台会输出</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">    Hello beforeDestroy</span></span><br><span class="line"><span class="comment">    Hello destroyed</span></span><br><span class="line"><span class="comment">    undefined</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">    说明beforeDestroy()和destroyed()两个函数分别先后得到了执行，同时销毁了test实例   */</span></span><br><span class="line"></span><br><span class="line">    beforeUpdate: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="keyword">this</span>.message + <span class="string">' beforeUpdate'</span>)</span><br><span class="line">    &#125;,</span><br><span class="line">    updated: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="keyword">this</span>.message + <span class="string">' updated'</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">/*   beforeUpdate和updated函数分别是当数据改变时，会在改变前和改变后分别执行的两个函数，</span></span><br><span class="line"><span class="comment">    此时如果我们在控制台执行"test.message = 'Hi'"，控制台会输出</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">    Hi beforeUpdate</span></span><br><span class="line"><span class="comment">    Hi updated</span></span><br><span class="line"><span class="comment">    "Hi"</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">    说明beforeUPdate()和updated()两个函数分别在数据改变前后得到了执行   */</span></span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;生命周期函数就是vue实例在某一个时间点会自动执行的函数，我们可以通过在vue实例运行过程中的不同时间点来嵌入不同的函数，以此来实现相应的功能。</p>
]]></content>
      <categories>
        <category>技术</category>
        <category>Vue.js</category>
      </categories>
      <tags>
        <tag>Vue.js</tag>
      </tags>
  </entry>
  <entry>
    <title>Parallels配置虚拟机网络（宿主机ip不固定，虚拟机ip固定）</title>
    <url>/2019/parallels-network.html</url>
    <content><![CDATA[<p>&emsp;&emsp;最近我在Parallels里安装了一台ubuntu的虚拟机，因为平常都是用宿主机ssh远程到虚拟机上使用的，所以在配置虚拟机网络这一块就首先选择了桥接模式。Parallels里的桥接模式下，<strong>虚拟机可以和宿主机通信，可以和与宿主机同一局域网内且同一网段的其它设备通信，多个虚拟机之间也可以相互通信，也可以连接互联网</strong>，等于为虚拟机新分配了一个局域网地址，一般正常使用的话，我都是采用这种模式。但是这种模式下有一个问题，因为我用的是笔记本电脑，经常是在家和公司之间或者其它地方使用。这样当我的笔记本（宿主机）网络环境变了以后，我的虚拟机ip地址也会改变。虽然可以在虚拟机内设置固定ip，但宿主机的网段一旦改变，之前虚拟机设置的固定ip如果和宿主机不处于同一网段下的话，还是无法实现通信。举个栗子：</p>
<a id="more"></a>
<blockquote>
<p>比如在公司的时候，宿主机被分配到的ip地址是 <strong>192.168.xxx.3</strong><br>这时如果采用桥接模式，虚拟机被分配到的ip地址是 <strong>192.168.xxx.4</strong> ，这时是没问题的。<br>可是如果回了家，宿主机的被分配到的ip地址是 <strong>192.168.yyy.3</strong></p>
</blockquote>
<p>&emsp;&emsp;这种时候要么虚拟机不固定ip，网段也变为了yyy，我每次开机都需要先查看虚拟机的ip地址，然后才能远程连接，这样太麻烦了。固定ip的话，因为宿主机的网段会变，而且不固定，又无法实现通信。所以这种模式显然不适合宿主机ip不固定的情况。</p>
<p>&emsp;&emsp;我先来整理一下我的需求：</p>
<ol>
<li>宿主机和虚拟机之间可以正常通信。</li>
<li>多个虚拟机之间可以正常通信。</li>
<li>宿主机同一局域网下且网段相同的设备可以和虚拟机之间实现通信。</li>
<li>虚拟机可以正常连接互联网。</li>
</ol>
<p>&emsp;&emsp;经过一番谷歌大法后，终于找到了解决方案。</p>
<p>&emsp;&emsp;首先在Parallels的“偏好设置”–“网络”中，勾选<strong>在系统偏好设置中显示</strong>和<strong>启用IPv4 DHCP</strong>，设置好起始地址和结束地址，这里可以重新设置一个网段（当然也可以点击做左边网卡列表的+号新加一块网卡来设置）。勾选了在系统偏好设置中显示这个选项以后，在你的mac中的“系统偏好设置”–“网络”里就可以看到这块网卡，里面有本机的ip地址。</p>
<p><img src="https://cdn.xiekaifei.com/image/2019/blog/jishu/2019-12-2410.26.46.png" alt="Parallels网络偏好设置"></p>
<p>&emsp;&emsp;然后添加端口转发。</p>
<p><img src="https://cdn.xiekaifei.com/image/2019/blog/jishu/2019-12-2410.47.35.png" alt="Parallels端口转发"></p>
<p>&emsp;&emsp;最后把虚拟机的网络配置为<strong>共享网络</strong>。</p>
<p><img src="https://cdn.xiekaifei.com/image/2019/blog/jishu/2019-12-2410.32.48.png" alt="Parallels设置虚拟机网络为共享模式"></p>
<p>&emsp;&emsp;这里解释一下我们都干了什么。首先我们添加了一个全新的192.168.8.xxx的网段，并把虚拟机的网络配置为了共享网络模式。<strong>这种模式等于宿主机新加了一块虚拟网卡给虚拟机使用。宿主机的网卡可以实现和同局域网下且同一网段的其它设备通信，同时宿主机的虚拟网卡也可以实现宿主机与虚拟机之间通信</strong>。我们还添加了端口转发，这一项设置是为了让和宿主机处于同一局域网下且同一网段的其它设备与虚拟机产生通信。直接通信是不行的，因为这些设备与虚拟机不处于同一网段（虚拟机处于新添加的192.168.8.xxx网段下），所以刚才设置的端口转发就派上了用场。上图设置了访问宿主机的<strong>ip+65532</strong>端口号，自动转发给我的名为“Ubuntu 18.04”的虚拟机的22端口，这样就可以通过ssh来直接远程我的Linux虚拟机。</p>
<p>&emsp;&emsp;最后再把虚拟机的ip固定一下，这样每次ssh连接虚拟机的时候就可以使用同一个ip地址了。</p>
<p>&emsp;&emsp;刚才我们转发到的是虚拟机的22端口，当然如果想让虚拟机充当web服务器的角色的话，也可以再添加一个80端口，就可以实现局域网内的web访问了。（宿主机可以直接访问虚拟机的ip地址，和宿主机处于同一局域网且同一网段的其它设备无法直接访问，需要访问宿主机的ip地址+转发前的端口号）</p>
<p><img src="https://cdn.xiekaifei.com/image/2019/blog/jishu/2019-12-2410.56.44.png" alt="转发访问虚拟机80端口"></p>
]]></content>
      <categories>
        <category>技术</category>
        <category>虚拟机</category>
      </categories>
      <tags>
        <tag>虚拟机</tag>
        <tag>Parallels</tag>
        <tag>网络</tag>
      </tags>
  </entry>
  <entry>
    <title>初建博客</title>
    <url>/2018/hello-blog.html</url>
    <content><![CDATA[<p>&emsp;&emsp;之前一直有想搭建一套个人博客系统的想法，正好手里有一台闲置的VPS，经过两天的折腾，在踩了无数个坑后，终于算是把博客初步建立起来。从Linux、Nginx，到MySql、PHP，慢慢体会到了建站的乐趣。以后会不定期更新博文，用来记录一些生活上的事情以及个人学习的过程。</p>
]]></content>
      <categories>
        <category>生活</category>
      </categories>
  </entry>
  <entry>
    <title>更自由的WordPress小工具插件！</title>
    <url>/2018/wp-plugin.html</url>
    <content><![CDATA[<p>&emsp;&emsp;最近在编辑博文的时候，发现默认的wordpress主题，文章页面在加了博客边栏小工具后变的很窄，导致有图片的文章会显示的很小，横向的图片更是显示的很难看。于是google了一下，发现有一款名为”Widget Logic”插件可以做到在指定的页面隐藏小工具组件，于是马上下载安装，试了后发现达不到想要的效果。这种插件只能在指定页面隐藏小工具，让其不显示，但并非完全去除，页面的布局还是没有更改，看起来还是很丑。<br><a id="more"></a><br>&emsp;&emsp;后来偶然发现一款名字叫<strong>“Widget Options”</strong>的插件，可以设置特定页面小工具的隐藏或显示。并且与上面那一款不同，是完全把指定页面的小工具去除掉，文章页面排版看起来终于正常。</p>
]]></content>
      <categories>
        <category>技术</category>
        <category>其他</category>
      </categories>
      <tags>
        <tag>WordPress</tag>
      </tags>
  </entry>
  <entry>
    <title>vim常用编辑快捷键</title>
    <url>/2019/vim-edit.html</url>
    <content><![CDATA[<p>&emsp;&emsp;在使用终端的时候，免不了会使用vi/vim编辑器来编辑文件。下面罗列一些编辑时的常用快捷键，可以提高使用效率。完整版的vim使用教程见最底部的引用链接。</p>
<a id="more"></a>
<table>
<thead>
<tr>
<th>按键</th>
<th>操作</th>
</tr>
</thead>
<tbody>
<tr>
<td>h或左箭头键</td>
<td>光标向左移动一个字符</td>
</tr>
<tr>
<td>j或下箭头键</td>
<td>光标向下移动一个字符</td>
</tr>
<tr>
<td>k或向上箭头键</td>
<td>光标向上移动一个字符</td>
</tr>
<tr>
<td>l或向右箭头键</td>
<td>光标向右移动一个字符</td>
</tr>
<tr>
<td>行数后跟移动按键组合键，如30j</td>
<td>光标向下移动30行</td>
</tr>
<tr>
<td>Ctrl + f</td>
<td>屏幕向下移动一页</td>
</tr>
<tr>
<td>Ctrl + b</td>
<td>屏幕向上移动一页</td>
</tr>
<tr>
<td>+</td>
<td>光标移动到非空格符的下一行</td>
</tr>
<tr>
<td>-</td>
<td>光标移动到非空格符的上一行</td>
</tr>
<tr>
<td>n后跟space键</td>
<td>n表示数字，表示光标向右移动n个字符</td>
</tr>
<tr>
<td>0或Home键</td>
<td>光标移动到这一行最前面的字符处</td>
</tr>
<tr>
<td>$或End键</td>
<td>光标移动到这一行最后面的字符处</td>
</tr>
<tr>
<td>H</td>
<td>光标移动到这个屏幕的最上方那一行的第一个字符</td>
</tr>
<tr>
<td>M</td>
<td>光标移动到这个屏幕的中央那一行的第一个字符</td>
</tr>
<tr>
<td>L</td>
<td>光标移动到这个屏幕的最下方那一行的第一个字符</td>
</tr>
<tr>
<td>G</td>
<td>移动到这个档案的最后一行(常用)</td>
</tr>
<tr>
<td>nG</td>
<td>n 为数字。移动到这个档案的第 n 行。例如 20G 则会移动到这个档案的第 20 行(可配合 :set nu)</td>
</tr>
<tr>
<td>gg</td>
<td>移动到这个档案的第一行，相当于 1G 啊！ (常用)</td>
</tr>
<tr>
<td>n后跟Enter键</td>
<td>n 为数字。光标向下移动 n 行(常用)</td>
</tr>
</tbody>
</table>
<p>完整快捷键列表见<a href="https://www.runoob.com/linux/linux-vim.html" target="_blank" rel="noopener">vi/vim使用教程</a></p>
]]></content>
      <categories>
        <category>技术</category>
        <category>工具</category>
      </categories>
      <tags>
        <tag>vi/vim</tag>
      </tags>
  </entry>
  <entry>
    <title>如何将一个JavaScript数组打乱</title>
    <url>/2020/js-array-order.html</url>
    <content><![CDATA[<p>&emsp;&emsp;如何将一个js数组的顺序打乱并且又能做到完全随机打乱呢？今天在网上看到了这样一个算法，觉得思路很不错，写上来记录一下。</p>
<p>&emsp;&emsp;比如我们有这样一个数组：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> arr = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>]</span><br></pre></td></tr></table></figure>
<a id="more"></a>
<p>&emsp;&emsp;实现思路是这样的：</p>
<ol>
<li>首先用数组的最后一个值<code>arr[arr.length - 1]</code>来与前面的5个值中随机抽取一个值交换位置。</li>
<li>在上一步中我们已经把数组末尾的值调换了位置，接着按照同样的方法对倒数第二个值进行同样的操作，再调换一次位置。</li>
<li>对下一个值执行相同的操作，直到全部执行完毕（执行到数组的第二个值就可以了）。</li>
</ol>
<p>&emsp;&emsp;下面我们用代码来实现一下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> arr = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>]</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">randomSortArr</span>(<span class="params">arr</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// 生成0到max之间的随机数</span></span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">exchange</span>(<span class="params">max</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">Math</span>.floor(<span class="built_in">Math</span>.random() * (max + <span class="number">1</span>))</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = arr.length - <span class="number">1</span>; i &gt; <span class="number">0</span>; i--) &#123;</span><br><span class="line">    <span class="keyword">let</span> randomIndex = exchange(i - <span class="number">1</span>)</span><br><span class="line">    <span class="keyword">let</span> num = arr[i]</span><br><span class="line">    arr[i] = arr[randomIndex]</span><br><span class="line">    arr[randomIndex] = num</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> arr</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(randomSortArr(arr))  <span class="comment">// [2, 6, 5, 1, 4, 3]</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;结果如我们所愿，数组被成功打乱了。下面再上一个优化了代码数量后的版本（方法一样，只是简化了代码量，第一个方法便于读者理解）：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> arr = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>]</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">randomSortArr</span>(<span class="params">arr</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = arr.length - <span class="number">1</span>; i &gt; <span class="number">0</span>; i--) &#123;</span><br><span class="line">    <span class="keyword">let</span> randomIndex = <span class="built_in">Math</span>.floor(<span class="built_in">Math</span>.random() * i)</span><br><span class="line">    [arr[i], arr[randomIndex]] = [arr[randomIndex], arr[i]]</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> arr</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(randomSortArr(arr))</span><br></pre></td></tr></table></figure>
<p>算法转载自<a href="https://www.zhihu.com/question/68330851/answer/266506621" target="_blank" rel="noopener">知乎</a></p>
]]></content>
      <categories>
        <category>技术</category>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>javascript</tag>
      </tags>
  </entry>
  <entry>
    <title>某天，重庆的早晨</title>
    <url>/2018/chongqing-morning.html</url>
    <content><![CDATA[<p>&emsp;&emsp;来重庆一周了，今天周末，起了个大早看看这座城市的一角。重庆这边的雨一下就是一周，来重庆的这一周基本每天都有雨，下下停停，基本上都是牛毛细雨，这种程度的小雨我几乎是不打伞的，淋在身上凉凉的还挺舒服。今天早上没有下雨，但还是一直阴天。</p>
<p>&emsp;&emsp;我租住在一个09年的老小区，小区里很多老人和孩子，环境倒也说的过去。重庆这边的楼房都蛮高，一座连着一座。下楼先拍一张，再去外面转转。</p>
<p><img src="https://cdn.xiekaifei.com/image/blog/shenghuo/20180922IMG_20180922_070135R.jpg" alt="住处楼下"><br><a id="more"></a></p>
<p>&emsp;&emsp;从我住的小区出来，穿过一条商业街，马路对面就是公司了。公司楼下是重庆奥体中心，昨天下班的时候看到奥体中心前的广场在搭台子，以为是中秋节要有什么活动，早上出来一看已经弄好了，原来是在搞车展。</p>
<p><img src="https://cdn.xiekaifei.com/image/blog/shenghuo/20180922IMG_20180922_070852R.jpg" alt="奥体中心门前车展"></p>
<p>重庆轨道交通2号线：</p>
<p><img src="https://cdn.xiekaifei.com/image/blog/shenghuo/20180922IMG_20180922_072659R.jpg" alt="重庆轨道交通2号线"></p>
<p><img src="https://cdn.xiekaifei.com/image/blog/shenghuo/20180922IMG_20180922_072653R.jpg" alt="重庆轨道交通2号线"></p>
<p>&emsp;&emsp;下面来说说重庆的轨道交通。我一直觉得重庆轨道交通的发展还是相当不错的，无论从线路密集程度还是从开通时间（2004年）来说，在中国都算是排名靠前的。轨道交通有轻轨和地铁，有一种说法是地上的叫轻轨，地下的叫地铁，这种说法是完全错误的。想必大多数人都不太清楚这两者间的区别，我自己也是经常把两者搞混。下面先贴两段维基百科里定义的轻轨。</p>
<blockquote>
<p>&emsp;&emsp;中华人民共和国所指的“轻轨”曾经与英文中的“Light rail”具有完全不同的含义。中华人民共和国建设部与国家发展和改革委员会1999年发布的《城市快速轨道交通工程项目建设标准》规定：按线路远期单向客运能力（断面运量），城市快速轨道交通线路分为三类线路运能等级。单向运能3.0至7.0万人次的线路为地铁，单向运能1.0至3.0万人次的线路为轻轨。</p>
</blockquote>
<blockquote>
<p>&emsp;&emsp;2008年重新修订的《城市轨道交通工程项目建设标准》（建标104-2008）取消了“轻轨”与“地铁”的区分，仅根据运能将线路分为Ⅰ、Ⅱ、Ⅲ、Ⅳ四个等级，并指出：“Ⅰ、Ⅱ、Ⅲ级线路是全封闭快速系统，采用独立的专用轨道和信号，高密度运行。Ⅳ级线路具有专用轨道和部分信号的中低运量系统，但部分路段设置平交道口。”</p>
</blockquote>
<p>&emsp;&emsp;所以，按照目前的建设标准来说，已经没有轻轨与地铁之分了，只有运量等级的差别，地铁的运量是要高于轻轨的。比如我拍的重庆轨道交通2号线，只有4节车厢（每趟车的车厢数可能不同），对比我之前经常坐的北京地铁昌平线的4组4节车厢来说，是要短很多的，同时客运量也少的多。不过重庆这边地势较为复杂，2号线修建地铁难度较大，只能采用修建轻轨的解决方案。不过轻轨也有轻轨的好处，重庆轨道交通2号线还是创造了很多世界之最的。</p>
<p>以下内容引用自百度百科“重庆轨道交通二号线”词条。</p>
<blockquote>
<p>&emsp;&emsp;采用跨座式单轨系统，正线最小曲线半径150m，最大限坡60‰，较好地适应了重庆市区地形起伏、道路狭窄的特点。2号线是中国在运营城市交通中落差最大的一条，总高程落差达115米，创下世界城市轨道交通之最。</p>
<p>&emsp;&emsp;采用橡胶轮（内充高压氮气）+PC轨道梁，运行噪声及震动远远低于采用钢轮钢轨的传统地铁，因此2号线通过的居民区均不再另外安装声屏障。经过5年的运行，被誉为「中国最安静的轨道交通线」。</p>
</blockquote>
<p>时代天街：</p>
<p><img src="https://cdn.xiekaifei.com/image/blog/shenghuo/20180922IMG_20180922_080631R.jpg" alt="时代天街"></p>
<p>&emsp;&emsp;时代天街这里看到一处jeep的展台，工作人员正在安装用来展示jeep越野能力的交叉轴。</p>
<p><img src="https://cdn.xiekaifei.com/image/blog/shenghuo/20180922IMG_20180922_075729R.jpg" alt="交叉轴"></p>
<p>&emsp;&emsp;贴一张公司办公楼的图（最瘦最高那个就是)。</p>
<p><img src="https://cdn.xiekaifei.com/image/blog/shenghuo/20180922IMG_20180922_081413R.jpg" alt="公司办公楼"></p>
<p>&emsp;&emsp;往回家走的时候路过菜市场，偷拍了一张（没好意思进去拍）。</p>
<p><img src="https://cdn.xiekaifei.com/image/blog/shenghuo/20180922IMG_20180922_083715R.jpg" alt="菜市场"></p>
<p>&emsp;&emsp;周末早晨的人还是挺多的，有正在跑步运动的人，也有跳广场舞锻炼的大妈，更有匆匆赶路上班的年轻人，感觉重庆还是一个挺有朝气的城市。</p>
<p>&emsp;&emsp;走了这么久也有点饿了，最后以一碗12块的肥肠小面结尾，香！</p>
<p><img src="https://cdn.xiekaifei.com/image/blog/shenghuo/20180922IMG_20180922_084531R.jpg" alt="肥肠小面"></p>
]]></content>
      <categories>
        <category>生活</category>
      </categories>
  </entry>
  <entry>
    <title>箭头函数里的this到底指向谁？</title>
    <url>/2020/js-this.html</url>
    <content><![CDATA[<p>&emsp;&emsp;关于箭头函数里的this这个问题一直很模糊，之前看了很多教程虽然知道了其行为，但是具体实现规则一直不太清晰。今天为了彻底搞懂这个问题，查了很多资料，感觉查到的很多例子不是很能清晰的描述这种行为。所以今天特地自己写一遍文章来说明一下。</p>
<a id="more"></a>
<h2 id="普通函数里的this"><a href="#普通函数里的this" class="headerlink" title="普通函数里的this"></a>普通函数里的this</h2><p>&emsp;&emsp;先来说一下普通函数里的this。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">1</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">test</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="keyword">this</span>.a)  <span class="comment">// this指向window，打印1</span></span><br><span class="line">&#125;</span><br><span class="line">test()</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;在普通函数里，this的值在定义时是不明确的，要等到调用时才能确定，谁调用就指向谁。上面代码里的<code>test()</code>是在全局作用域调用的，所以自然就指向<code>window</code>，控制台会打印1。</p>
<p>&emsp;&emsp;再来看一个例子：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">1</span></span><br><span class="line"><span class="keyword">var</span> obj = &#123;</span><br><span class="line">  a: <span class="number">2</span>,</span><br><span class="line">  test: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span>.a)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">obj.test()  <span class="comment">// 2</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;上面的代码我们把<code>test</code>函数定义在里对象<code>obj</code>里，调用的时候是用<code>obj</code>来调用的，所以<code>test</code>函数里的<code>this</code>自然就指向<code>obj</code>，最后函数会打印2。</p>
<p>&emsp;&emsp;再来看一个：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">1</span></span><br><span class="line"><span class="keyword">var</span> obj = &#123;</span><br><span class="line">  a: <span class="number">2</span>,</span><br><span class="line">  test: <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span>.a)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> w = obj.test</span><br><span class="line">w()  <span class="comment">// 1</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;上面的代码我们把<code>obj.test</code>函数里的代码赋值给了一个全局变量<code>w</code>，然后用全局变量<code>w</code>调用了<code>test</code>函数，所以这里的<code>this</code>指向的也是<code>window</code>。</p>
<p>&emsp;&emsp;还有一种情况：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">1</span></span><br><span class="line"><span class="keyword">var</span> obj = &#123;</span><br><span class="line">  a: <span class="number">2</span>,</span><br><span class="line">  test: <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">fn</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="keyword">this</span>.a)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> fn()</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">obj.test()  <span class="comment">// 1</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;这里来看<code>obj.test</code>方法，这个方法里我们创建了一个<code>fn</code>函数，最后将<code>fn</code>函数的结果作为<code>obj.test</code>的结果返回，这里的<code>fn</code>是用<code>return</code>方法返回的，并没有被其它对象直接调用，普通函数里this也并不会被继承，所以这里的<code>this</code>指向全局的<code>window</code>。</p>
<p>&emsp;&emsp;<code>setTimeout</code>方法和<code>setInterval</code>方法中的this均指向window，如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">1</span></span><br><span class="line"><span class="keyword">var</span> obj = &#123;</span><br><span class="line">  a: <span class="number">2</span>,</span><br><span class="line">  test: <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    setTimeout(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="keyword">this</span>.a)</span><br><span class="line">    &#125;, <span class="number">1000</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">obj.test()  <span class="comment">// 1</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;上面的几种情况就是普通函数里this的指向。</p>
<p>&emsp;&emsp;使用<code>call()</code>、<code>apply()</code>、<code>bind()</code>等方法可以手动改变普通函数里this的指向，在本文里就不详细叙述了。</p>
<h2 id="箭头函数里的this"><a href="#箭头函数里的this" class="headerlink" title="箭头函数里的this"></a>箭头函数里的this</h2><p>先说结论：</p>
<ul>
<li>箭头函数不会创建自己的<code>this，它只会从自己的作用域链的上一层继承this</code>（结论出自<a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Functions/Arrow_functions" target="_blank" rel="noopener">MDN - 箭头函数</a>）</li>
<li>使用call()、apply()、bind()等方法并不会<strong>直接</strong>修改箭头函数内的this</li>
<li>箭头函数的this是在创建时就已经确定了</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">1</span></span><br><span class="line"><span class="keyword">var</span> obj = &#123;</span><br><span class="line">  a: <span class="number">2</span>,</span><br><span class="line">  test: <span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span>.a)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">obj.test()  <span class="comment">// 1</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;这里的this是在箭头函数里的，所以会从自己的作用域链的上一层，也就是<code>obj</code>对象来继承this。最后一行<code>obj</code>是在全局中调用的，所以<code>obj</code>的this指向<code>window</code>，其结果打印的是1。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">a</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">b</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span> === <span class="built_in">window</span>)  <span class="comment">// true</span></span><br><span class="line">    <span class="keyword">var</span> c = <span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="keyword">this</span>)  <span class="comment">// window</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> c()</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> b()</span><br><span class="line">&#125;</span><br><span class="line">a()</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;上面的这个例子，c是箭头函数，所以c里的this继承自己的作用域链的上一层b，b是直接return返回的结果，this指向window。</p>
<p>&emsp;&emsp;我们再把上面的例子修改一下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = &#123;</span><br><span class="line">  a: <span class="string">'xxx'</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">a</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">b</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span> === obj)  <span class="comment">// true</span></span><br><span class="line">    <span class="keyword">var</span> c = <span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="keyword">this</span>)  <span class="comment">// obj对象</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> c()</span><br><span class="line">  &#125;</span><br><span class="line">  b.call(obj)</span><br><span class="line">&#125;</span><br><span class="line">a()</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;上面的例子我们手动用<code>call()</code>方法把b的this指向更改为了obj，通过打印出来的结果再一次印证了<strong>箭头函数不会创建自己的this，它只会从自己的作用域链的上一层继承this</strong>这一结论。</p>
]]></content>
      <categories>
        <category>技术</category>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>javascript</tag>
        <tag>ES6</tag>
        <tag>箭头函数</tag>
      </tags>
  </entry>
  <entry>
    <title>重新封装JavaScript里的String.indexOf()方法</title>
    <url>/2019/js-indexOF.html</url>
    <content><![CDATA[<h2 id="indexOF-方法"><a href="#indexOF-方法" class="headerlink" title="indexOF()方法"></a>indexOF()方法</h2><p>&emsp;&emsp;JavaScript的String对象中有一个indexOf()方法，这个方法接收一个指定字符串参数（必须）和查找位置参数（可选）。该方法返回指定字符串在字符串中第一次出现的位置，如果没有出现，则返回”-1”。如果有传查找位置参数，则从传入的位置查起，如果没有传位置参数默认为0，即从字符串的开头查起。</p>
<p>&emsp;&emsp;说完了这个函数的功能，那么我们如何自己封装一个一样功能的方法呢？</p>
<a id="more"></a>
<p>&emsp;&emsp;先说思路。首先当传入了指定字符串后，我们可以将指定字符串的第一个字符作为一个变量保存起来；然后用这个变量在循环内依次和字符串相比较；如果相同，则进一步比较相同长度下，是否完全相同即可。最后返回相应的结果数值。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 重新封装String.indexOf函数</span></span><br><span class="line"><span class="comment">// 调用方式与原生的方法略有不同，将字符串变为函数的第一个参数，其它不变</span></span><br><span class="line"><span class="comment">// str为字符串；findStr为指定的字符串；index为查找位置，不传则默认为0</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">indexOF</span>(<span class="params">str, findStr, index = <span class="number">0</span></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="keyword">typeof</span> str === <span class="string">'string'</span> &amp;&amp; <span class="keyword">typeof</span> findStr === <span class="string">'string'</span> &amp;&amp; <span class="keyword">typeof</span> index === <span class="string">'number'</span>) &#123;</span><br><span class="line">    <span class="comment">// 将指定字符串的第一个字符作为一个变量保存</span></span><br><span class="line">    <span class="keyword">let</span> firstStr = findStr[<span class="number">0</span>]</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">let</span> i = index; i &lt; str.length; i++) &#123;</span><br><span class="line">      <span class="comment">// findStr与str中查询部分首字符相同则进一步查询</span></span><br><span class="line">      <span class="keyword">if</span> (str[i] === firstStr) &#123;</span><br><span class="line">        <span class="comment">// 完全相同</span></span><br><span class="line">        <span class="keyword">if</span> (str.substr(i, findStr.length) === findStr) &#123;</span><br><span class="line">          <span class="keyword">return</span> i</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">          <span class="keyword">continue</span></span><br><span class="line">        &#125;</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">continue</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span></span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    alert(<span class="string">'function indexOF的传入参数格式有误，请重新传入正确的参数执行查询！'</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">undefined</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;写完了来调用试验一下结果（调用方式与原生的方法略有不同）：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(indexOF(<span class="string">'xiekaifei'</span>, <span class="string">'i'</span>))     <span class="comment">// 1</span></span><br><span class="line"><span class="built_in">console</span>.log(indexOF(<span class="string">'xiekaifei'</span>, <span class="string">'i'</span>, <span class="number">3</span>))  <span class="comment">// 5</span></span><br><span class="line"><span class="built_in">console</span>.log(indexOF(<span class="string">'xiekaifei'</span>, <span class="string">'ok'</span>))    <span class="comment">// -1</span></span><br></pre></td></tr></table></figure>
<h2 id="strFind-方法"><a href="#strFind-方法" class="headerlink" title="strFind()方法"></a>strFind()方法</h2><p>&emsp;&emsp;实现了这个方法后我们再来实现一个查询所有位置的方法。实现的功能是：<strong>传入字符串和指定的字符串，返回所有出现位置的数组，未找到时则返回-1</strong>。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">strFind</span>(<span class="params">str, findStr</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="keyword">typeof</span> str === <span class="string">'string'</span> &amp;&amp; <span class="keyword">typeof</span> findStr === <span class="string">'string'</span>) &#123;</span><br><span class="line">    <span class="keyword">let</span> arr = []</span><br><span class="line">    <span class="keyword">let</span> index = <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; str.length; i++) &#123;</span><br><span class="line">      <span class="comment">// 调用刚刚封装好的indexOF()方法查询，并将查询结果暂时保存给num参数</span></span><br><span class="line">      <span class="keyword">let</span> num = indexOF(str, findStr, index)</span><br><span class="line">      <span class="keyword">if</span> (num &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">        arr.push(num)</span><br><span class="line">        index = ++num</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">break</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (arr.length &gt; <span class="number">1</span>) &#123;</span><br><span class="line">      <span class="comment">// 返回所有出现的位置的数组</span></span><br><span class="line">      <span class="keyword">return</span> arr</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (arr.length === <span class="number">1</span>) &#123;</span><br><span class="line">      <span class="comment">// 只出现一次的话直接返回位置</span></span><br><span class="line">      <span class="keyword">return</span> arr[<span class="number">0</span>]</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="comment">// 未出现返回-1</span></span><br><span class="line">      <span class="keyword">return</span> <span class="number">-1</span></span><br><span class="line">    &#125;</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    alert(<span class="string">'function strFind的传入参数格式有误，期待两个字符串参数，请重新传入正确的参数执行查询！'</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">undefined</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;再调用一下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(strFind(<span class="string">'xiekaifei'</span>, <span class="string">'i'</span>))   <span class="comment">// [1, 5, 8]</span></span><br><span class="line"><span class="built_in">console</span>.log(strFind(<span class="string">'xiekaifei'</span>, <span class="string">'fe'</span>))  <span class="comment">// [6]</span></span><br><span class="line"><span class="built_in">console</span>.log(strFind(<span class="string">'xiekaifei'</span>, <span class="string">'ok'</span>))  <span class="comment">// -1</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;大功告成！</p>
]]></content>
      <categories>
        <category>技术</category>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>javascript</tag>
      </tags>
  </entry>
</search>
